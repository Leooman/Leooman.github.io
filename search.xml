<?xml version="1.0" encoding="utf-8"?>
<search> 
  
    
    <entry>
      <title><![CDATA[.htaccess文件详解]]></title>
      <url>/2018/01/06/htaccess/</url>
      <content type="html"><![CDATA[<blockquote>
<p><strong>.htaccess的基本作用</strong></p>
</blockquote>
<p>.htaccess是一个纯文本文件，它里面存放着Apache服务器配置相关的指令。</p>
<p>.htaccess主要的作用有：<strong>URL重写、自定义错误页面、MIME类型配置以及访问权限控制等</strong>。</p>
<p>主要体现在伪静态的应用、图片防盗链、自定义404错误页面、阻止/允许特定IP/IP段、目录浏览与主页、禁止访问指定文件类型、文件密码保护等。</p>
<p>.htaccess的用途范围主要针对当前目录。</p>
<blockquote>
<p><strong>如何启用.htaccess的配置</strong></p>
</blockquote>
<p>启用.htaccess，需要修改httpd.conf文件，启用AllowOverride，并可以用AllowOverride限制特定命令的使用。</p>
<pre><code>&lt;Directory /&gt;
Options FollowSymLinks
AllowOverride None
&lt;/Directory&gt;
</code></pre><p>改为：</p>
<pre><code>&lt;Directory /&gt;
Options FollowSymLinks
AllowOverride All
&lt;/Directory&gt;
</code></pre><p>如果需要使用.htaccess以外的其他文件名，可以用AccessFileName指令来改变。例如，需要使用.config ，则可以在服务器配置文件中按以下方法配置：</p>
<pre><code>AccessFileName .config
</code></pre><blockquote>
<p><strong>.htaccess访问控制</strong></p>
</blockquote>
<p>限制用户访问一些关键目录</p>
<pre><code>&lt;Files  ~ &quot;^.*\.([Ll][Oo][Gg])|([eE][xX][eE])&quot;&gt;
 Order allow,deny
 Deny from all
&lt;/Files&gt;
</code></pre><ul>
<li>Files后的波浪线表示启用“正则表达式”</li>
<li>Order命令：通过Allow,Deny参数，Apache首先找到并应用Allow命令，然后应用Deny命令，以阻止所有访问，也可以使用Deny,Allow</li>
</ul>
<blockquote>
<p><strong>URL重写</strong></p>
</blockquote>
<pre><code># 将 RewriteEngine 模式打开 
RewriteEngine On 
# Rewrite 系统规则请勿修改 
RewriteRule ^p/([0-9]+)\.html$ index.php?post_id=$1
RewriteRule ^u-(username|uid)-(.+)\.html$ space.php?$1=$2
</code></pre><blockquote>
<p><strong>配置错误页面</strong></p>
</blockquote>
<pre><code># custom error documents
ErrorDocument 401 /err/401.php
ErrorDocument 403 /err/403.php
ErrorDocument 404 /err/404.php
ErrorDocument 500 /err/500.php
</code></pre><blockquote>
<p><strong>常用命令及配置</strong></p>
</blockquote>
<ul>
<li>禁止显示目录列表</li>
</ul>
<p>有些时候，由于某种原因，你的目录里没有index文件，这意味着当有人在浏览器地址栏键入了该目录的路径，该目录下所有的文件都会显示出来，这会给你的网站留下安全隐患。</p>
<p>为避免这种情况（而不必创建一堆的新index文件），你可以在你的.htaccess文档中键入以下命令，用以阻止<br>目录列表的显示：</p>
<pre><code>Options -Indexes
</code></pre><ul>
<li>阻止/允许特定的IP地址</li>
</ul>
<p>某些情况下，你可能只想允许某些特定IP的用户可以访问你的网站，或者想封禁某些特定的IP地址（例如：将低级用户隔离于你的信息版面外）。当然，这只在你知道你想拦截的IP地址时才有用，然而现在网上的大多数用户都使用动态IP地址，所以这并不是限制使用的常用方法。</p>
<p>使用以下命令封禁一个IP地址(只指明了其中的几个，则可以封禁整个网段的地址)：</p>
<pre><code>deny from 000.000.000.000
</code></pre><p>使用以下命令允许一个IP地址访问网站(只指明了其中的几个，则可以允许整个网段的地址)：</p>
<pre><code>allow from 000.000.000.000
</code></pre><p>阻止所有人访问该目录，则可以使用(并不影响脚本程序使用这个目录下的文档)：</p>
<pre><code>deny from all
</code></pre><ul>
<li>替换index文件</li>
</ul>
<p>也许你不想一直使用index.htm或index.html作为目录的索引文件。如果你的站点使用PHP文件，你可能会想使用 index.php来作为该目录的索引文档。当然也不必局限于“index”文档，如果你愿意，使用.htaccess你甚至能够设置 foofoo.balh来作为你的索引文档！</p>
<p>这些互为替换的索引文件可以排成一个列表，服务器会从左至右进行寻找，检查哪个文档在真实的目录中存在。如果一个也找不到，它将会把目录列表显示出来（除非你已经关闭了显示目录文件列表）。</p>
<pre><code>DirectoryIndex index.php index.php3 messagebrd.pl index.html index.htm
</code></pre><ul>
<li>重定向(rewrite)</li>
</ul>
<p>.htaccess 最有用的功能之一就是将请求重定向到同站内或站外的不同文档。这在你改变了一个文件名称，但仍然想让用户用旧地址访问到它时，变的极为有用。另一个应用是重定向到一个长URL。以下是一个重定向文件的例子：</p>
<pre><code>Redirect /location/from/root/file.ext http:///new/file/location.xyz
</code></pre><p>你也可以使用.htaccess重定向整个网站的目录。假如你的网站上有一个名为olddirectory的目录，并且你已经在一个新网站http: ///newdirectory/上建立了与上相同的文档，你可以将旧目录下所有的文件做一次重定向而不必一一声明：</p>
<pre><code>Redirect /olddirectory http:　///newdirectory
</code></pre><blockquote>
<p><strong>安全配置</strong></p>
</blockquote>
<ul>
<li>.htaccess链接盗用保护</li>
</ul>
<pre><code>RewriteBase /  
RewriteCond %{HTTP_REFERER} !^$  
RewriteCond %{HTTP_REFERER} !^http://(www.)?aqee.net/.*$ [NC]  
RewriteRule .(gif|jpg|swf|flv|png)$ /feed/ [R=302,L] 
</code></pre><ul>
<li>防黑客</li>
</ul>
<pre><code>RewriteEngine On  

#proc/self/environ
RewriteCond %{QUERY_STRING} proc/self/environ [OR]  

#阻止脚本企图通过URL修改mosConfig值  
RewriteCond %{QUERY_STRING} mosConfig_[a-zA-Z_]{1,21}(=|%3D) [OR]  

#阻止脚本通过URL传递的base64_encode垃圾信息  
RewriteCond %{QUERY_STRING} base64_encode.*(.*) [OR]  

#阻止在URL含有&lt;script&gt;标记的脚本  
RewriteCond %{QUERY_STRING} (&lt;|%3C).*script.*(&gt;|%3E) [NC,OR]  

#阻止企图通过URL设置PHP的GLOBALS变量的脚本  
RewriteCond %{QUERY_STRING} GLOBALS(=|[|%[0-9A-Z]{0,2}) [OR]  

#阻止企图通过URL设置PHP的_REQUEST变量的脚本  
RewriteCond %{QUERY_STRING} _REQUEST(=|[|%[0-9A-Z]{0,2})  

#把所有被阻止的请求转向到403禁止提示页面！  
RewriteRule ^(.*)$ index.php [F,L] 
</code></pre><ul>
<li>阻止访问你的 .htaccess 文件或者指定类型的文件</li>
</ul>
<pre><code># 保护你的 htaccess 文件  
&lt;Files .htaccess&gt;  
order allow,deny  
deny from all  
&lt;/Files&gt;  

# 阻止查看指定的文件  
&lt;Files secretfile.jpg&gt;  
order allow,deny  
deny from all  
&lt;/Files&gt;  

# 多种文件类型  
&lt;FilesMatch “.(htaccess|htpasswd|ini|phps|fla|psd|log|sh)$”&gt;  
 Order Allow,Deny  
Deny from all  
&lt;/FilesMatch&gt;
</code></pre><ul>
<li>禁止脚本执行</li>
</ul>
<pre><code># 禁止某些目录里的脚本执行权限  
AddHandler cgi-script .php .pl .py .jsp .asp .htm .shtml .sh .cgi  
Options -ExecCGI
</code></pre><blockquote>
<p><strong>其他常用配置</strong></p>
</blockquote>
<ul>
<li>屏蔽下载对话框</li>
</ul>
<pre><code>AddType application/octet-stream .pdf  
AddType application/octet-stream .zip  
AddType application/octet-stream .mov 
</code></pre><ul>
<li>省去www前缀</li>
</ul>
<pre><code>RewriteEngine On  
RewriteBase /  
RewriteCond %{HTTP_HOST} ^www.aqee.net [NC]  
RewriteRule ^(.*)$ http://aqee.net/$1 [L,R=301]
</code></pre><ul>
<li>压缩文件</li>
</ul>
<pre><code># 压缩 text, html, javascript, css, xml:  
AddOutputFilterByType DEFLATE text/plain  
AddOutputFilterByType DEFLATE text/html  
AddOutputFilterByType DEFLATE text/xml  
AddOutputFilterByType DEFLATE text/css  
AddOutputFilterByType DEFLATE application/xml  
AddOutputFilterByType DEFLATE application/xhtml+xml  
AddOutputFilterByType DEFLATE application/rss+xml  
AddOutputFilterByType DEFLATE application/javascript  
AddOutputFilterByType DEFLATE application/x-javascript 
</code></pre><ul>
<li>缓存文件</li>
</ul>
<pre><code>&lt;FilesMatch “.(flv|gif|jpg|jpeg|png|ico|swf|js|css|pdf)$”&gt;  
Header set Cache-Control “max-age=2592000″  
&lt;/FilesMatch&gt; 
</code></pre><ul>
<li>对某些文件类型禁止使用缓存</li>
</ul>
<pre><code># 显式的规定对脚本和其它动态文件禁止使用缓存  
&lt;FilesMatch “.(pl|php|cgi|spl|scgi|fcgi)$”&gt;  
Header unset Cache-Control  
&lt;/FilesMatch&gt; 
</code></pre>]]></content>
      
        
        <tags>
            
            <tag> 服务器 </tag>
            
        </tags>
        
    </entry>
    
    <entry>
      <title><![CDATA[ChangeDetectionCont]]></title>
      <url>/2017/11/01/ChangeDetectionCont/</url>
      <content type="html"><![CDATA[<h2 id="Change-Detection-Constants-4-21-2017-1-22-43-PM"><a href="#Change-Detection-Constants-4-21-2017-1-22-43-PM" class="headerlink" title="Change Detection Constants 4/21/2017 1:22:43 PM "></a>Change Detection Constants <span style="float:right;font-size:12px;line-height:40px;">4/21/2017 1:22:43 PM </span></h2><h3 id="ChangeDetectionStrategy"><a href="#ChangeDetectionStrategy" class="headerlink" title="ChangeDetectionStrategy"></a>ChangeDetectionStrategy</h3><p>Describes within the change detector which strategy will be used the next time change detection is triggered.</p>
<ul>
<li>OnPush = 0</li>
</ul>
<p><code>OnPush</code> means that the change detector’s mode will be set to <code>CheckOnce</code> during hydration.</p>
<ul>
<li>Default = 1</li>
</ul>
<p><code>Default</code> means that the change detector’s mode will be set to <code>CheckAlways</code> during hydration</p>
<h3 id="ChangeDetectorStatus"><a href="#ChangeDetectorStatus" class="headerlink" title="ChangeDetectorStatus"></a>ChangeDetectorStatus</h3><p>Describes the status of the detector</p>
<ul>
<li>CheckOnce = 0</li>
</ul>
<p><code>CheckedOnce</code> means that after calling detectChanges the mode of the change detector will become <code>Checked</code>.</p>
<ul>
<li>Checked = 1</li>
</ul>
<p><code>Checked</code> means that the change detector should be skipped until its mode changes to <code>CheckOnce</code></p>
<ul>
<li>CheckAlways = 2</li>
</ul>
<p><code>CheckAlways</code> means that after calling detectChanges the mode of the change detector will remain <code>CheckAlways</code></p>
<ul>
<li>Detached = 3</li>
</ul>
<p><code>Detached</code> means that the change detector sub tree is not a part of the main tree and should be skipped</p>
<ul>
<li>Errored = 4</li>
</ul>
<p><code>Errored</code> means that the change detector encountered an error checking a binding or calling a directive lifecycle method and is now in an inconsistent state. Change detectors in this state will no longer detect changes.</p>
<ul>
<li>Destroyed = 5</li>
</ul>
<p><code>Destroyed</code> means that the change detector is destroyed</p>
]]></content>
      
        
        <tags>
            
            <tag> Angular </tag>
            
        </tags>
        
    </entry>
    
    <entry>
      <title><![CDATA[ChangeDetectorRef]]></title>
      <url>/2017/10/31/ChangeDetectorRef/</url>
      <content type="html"><![CDATA[<h2 id="ChangeDetectorRef-4-21-2017-12-52-43-AM"><a href="#ChangeDetectorRef-4-21-2017-12-52-43-AM" class="headerlink" title="ChangeDetectorRef 4/21/2017 12:52:43 AM "></a>ChangeDetectorRef <span style="float:right;font-size:12px;line-height:40px;">4/21/2017 12:52:43 AM </span></h2><blockquote>
<p><strong>object method</strong></p>
</blockquote>
<pre><code>export class ChangeDetectorRef {
    markForCheck(): void,
    detach(): void,
    detectChanges(): void,
    checkNoChanges(): void,
    reattach(): void
}
</code></pre><blockquote>
<p><strong>markForCheck</strong></p>
</blockquote>
<p>Marks all {@link ChangeDetectionStrategy#OnPush} ancestors as to be checked.</p>
<h4 id="Example"><a href="#Example" class="headerlink" title="Example"></a>Example</h4><p><img src="https://github.com/Leooman/Angular/raw/master/images/changedetection.jpg" alt="markForCheck"></p>
<blockquote>
<p><strong>detach</strong></p>
</blockquote>
<p>Detaches the change detector from the change detector tree.</p>
<p>The detached change detector will not be checked until it is reattached.</p>
<p>This can also be used in combination with {@link ChangeDetectorRef#detectChanges} to implement local change detection checks.</p>
<blockquote>
<p><strong>detectChanges</strong></p>
</blockquote>
<p>Checks the change detector and its children.</p>
<p>This can also be used in combination with {@link ChangeDetectorRef#detach} to implement local change detection checks.</p>
<h4 id="Example-1"><a href="#Example-1" class="headerlink" title="Example"></a>Example</h4><p>For performance reasons,we want to check and update the list every five seconds. We can do that by detaching the component’s change detector and doing a local check every five seconds.</p>
<p><img src="https://github.com/Leooman/Angular/raw/master/images/detach.jpg" alt="detach"></p>
<blockquote>
<p><strong>checkNoChanges</strong></p>
</blockquote>
<p>Checks the change detector and its children, and throws if any changes are detected.</p>
<p>This is used in development mode to verify that running change detection doesn’t introduce other changes.</p>
<blockquote>
<p><strong>reattach</strong></p>
</blockquote>
<p>Reattach the change detector to the change detector tree.</p>
<p>This also marks OnPush ancestors as to be checked. This reattached change detector will be checked during the next change detection run.</p>
<h4 id="Example-2"><a href="#Example-2" class="headerlink" title="Example"></a>Example</h4><p><img src="https://github.com/Leooman/Angular/raw/master/images/reattach.jpg" alt="reattach"></p>
]]></content>
      
        
        <tags>
            
            <tag> Angular </tag>
            
        </tags>
        
    </entry>
    
    <entry>
      <title><![CDATA[Render]]></title>
      <url>/2017/10/31/Render/</url>
      <content type="html"><![CDATA[<h2 id="Render-4-21-2017-1-22-30-PM"><a href="#Render-4-21-2017-1-22-30-PM" class="headerlink" title="Render 4/21/2017 1:22:30 PM "></a>Render <span style="float:right;font-size:12px;line-height:40px;">4/21/2017 1:22:30 PM </span></h2><h4 id="RenderComponentType"><a href="#RenderComponentType" class="headerlink" title="RenderComponentType"></a>RenderComponentType</h4><pre><code>export class RenderComponentType {
    id: string;
    templateUrl: string;
    slotCount: number;
    encapsulation: ViewEncapsulation;
    styles: Array&lt;string | any[]&gt;;
    animations: {
        [key: string]: Function;
    };
    constructor(id: string, templateUrl: string, slotCount: number, encapsulation: ViewEncapsulation, styles: Array&lt;string | any[]&gt;, animations: {
        [key: string]: Function;
    });
}
</code></pre><h4 id="RenderDebugInfo"><a href="#RenderDebugInfo" class="headerlink" title="RenderDebugInfo"></a>RenderDebugInfo</h4><pre><code>export class RenderDebugInfo {
    injector: Injector;
    component: any;
    providerTokens: any[];
    references: {
        [key: string]: any;
    };
    context: any;
    source: string;
}
</code></pre><h4 id="DirectRenderer-Use-the-Renderer2-instead-in-version-4-0"><a href="#DirectRenderer-Use-the-Renderer2-instead-in-version-4-0" class="headerlink" title="DirectRenderer [Use the Renderer2 instead in version 4.0]"></a>DirectRenderer [Use the <code>Renderer2</code> instead in version 4.0]</h4><pre><code>export interface DirectRenderer {
    remove(node: any): void;
    appendChild(node: any, parent: any): void;
    insertBefore(node: any, refNode: any): void;
    nextSibling(node: any): any;
    parentElement(node: any): any;
}
</code></pre><h4 id="Renderer-Use-the-Renderer2-instead-in-version-4-0"><a href="#Renderer-Use-the-Renderer2-instead-in-version-4-0" class="headerlink" title="Renderer [Use the Renderer2 instead in version 4.0]"></a>Renderer [Use the <code>Renderer2</code> instead in version 4.0]</h4><ul>
<li><p><code>selectRootElement</code> (selectorOrNode: string | any, debugInfo?: RenderDebugInfo): any</p>
</li>
<li><p><code>createElement</code> (parentElement: any, name: string, debugInfo?: RenderDebugInfo): any</p>
</li>
<li><p><code>createViewRoot</code> (hostElement: any): any</p>
</li>
<li><p><code>createTemplateAnchor</code> (parentElement: any, debugInfo?: RenderDebugInfo): any</p>
</li>
<li><p><code>createText</code> (parentElement: any, value: string, debugInfo?: RenderDebugInfo): any</p>
</li>
<li><p><code>projectNodes</code> (parentElement: any, nodes: any[]): void</p>
</li>
<li><p><code>attachViewAfter</code> (node: any, viewRootNodes: any[]): void</p>
</li>
<li><p><code>detachView</code> (viewRootNodes: any[]): void</p>
</li>
<li><p><code>destroyView</code> (hostElement: any, viewAllNodes: any[]): void</p>
</li>
<li><p><code>listen</code> (renderElement: any, name: string, callback: Function): Function</p>
</li>
<li><p><code>listenGlobal</code> (target: string, name: string, callback: Function): Function</p>
</li>
<li><p><code>setElementProperty</code> (renderElement: any, propertyName: string, propertyValue: any): void</p>
</li>
<li><p><code>setElementAttribute</code> (renderElement: any, attributeName: string, attributeValue: string): void</p>
</li>
<li><p><code>setBindingDebugInfo</code> (renderElement: any, propertyName: string, propertyValue: string): void</p>
</li>
<li><p><code>setElementClass</code> (renderElement: any, className: string, isAdd: boolean): void</p>
</li>
<li><p><code>setElementStyle</code> (renderElement: any, styleName: string, styleValue: string): void</p>
</li>
<li><p><code>invokeElementMethod</code> (renderElement: any, methodName: string, args?: any[]): void</p>
</li>
<li><p><code>setText</code> (renderNode: any, text: string): void</p>
</li>
<li><p><code>animate</code> (element: any, startingStyles: AnimationStyles, keyframes: AnimationKeyframe[], duration: number, delay: number, easing: string): AnimationPlayer</p>
</li>
</ul>
<h4 id="Example"><a href="#Example" class="headerlink" title="Example"></a>Example</h4><p><img src="https://github.com/Leooman/Angular/raw/master/images/renderer.jpg" alt="Renderer"></p>
<blockquote>
<p><strong>RootRenderer</strong> [Use <code>RendererFactory2</code> instead in version 4.0]</p>
</blockquote>
<p>If you are implementing a custom renderer, you must implement this interface.</p>
<p>The default Renderer implementation is <code>DomRenderer</code>. Also available is <code>WebWorkerRenderer</code>.</p>
<pre><code>export declare abstract class RootRenderer {
    abstract renderComponent(componentType: RenderComponentType): Renderer;
}
</code></pre><h2 id="Version-4-0"><a href="#Version-4-0" class="headerlink" title="Version 4.0"></a>Version 4.0</h2><h3 id="RendererType2"><a href="#RendererType2" class="headerlink" title="RendererType2"></a>RendererType2</h3><pre><code>export interface RendererType2 {
    id: string;
    encapsulation: ViewEncapsulation;
    styles: (string | any[])[];
    data: {
        [kind: string]: any;
    };
}
</code></pre><h3 id="RendererFactory2"><a href="#RendererFactory2" class="headerlink" title="RendererFactory2"></a>RendererFactory2</h3><pre><code>export class RendererFactory2 {
    createRenderer(hostElement: any, type: RendererType2 | null): Renderer2;
}
</code></pre><h3 id="RendererStyleFlags2"><a href="#RendererStyleFlags2" class="headerlink" title="RendererStyleFlags2"></a>RendererStyleFlags2</h3><pre><code>export declare enum RendererStyleFlags2 {
    Important = 1,
    DashCase = 2,
}
</code></pre><h3 id="Renderer2"><a href="#Renderer2" class="headerlink" title="Renderer2"></a>Renderer2</h3><p>This field can be used to store arbitrary data on this renderer instance.</p>
<p>This is useful for renderers that delegate to other renderers.</p>
<ul>
<li>[readonly] <code>data</code>: {[key: string]: any}</li>
<li><code>destroy()</code>: void</li>
<li><code>createElement</code>(name: string, namespace?: string | null): any</li>
<li><code>createComment</code>(value: string): any</li>
<li><code>createText</code>(value: string): any</li>
<li><code>destroyNode</code>: ((node: any) =&gt; void) | null</li>
<li><code>appendChild</code>(parent: any, newChild: any): void</li>
<li><code>insertBefore</code>(parent: any, newChild: any, refChild: any): void</li>
<li><code>removeChild</code>(parent: any, oldChild: any): void</li>
<li><code>selectRootElement</code>(selectorOrNode: string | any): any</li>
<li><p><code>parentNode</code>(node: any): any</p>
</li>
<li><p><code>nextSibling</code>(node: any): any</p>
</li>
<li><code>setAttribute</code>(el: any, name: string, value: string, namespace?: string | null): void</li>
<li><code>removeAttribute</code>(el: any, name: string, namespace?: string | null): void</li>
<li><code>addClass</code>(el: any, name: string): void</li>
<li><code>removeClass</code>(el: any, name: string): void</li>
<li><code>setStyle</code>(el: any, style: string, value: any, flags?: RendererStyleFlags2): void</li>
<li><code>removeStyle</code>(el: any, style: string, flags?: RendererStyleFlags2): void</li>
<li><code>setProperty</code>(el: any, name: string, value: any): void</li>
<li><code>setValue</code>(node: any, value: string): void</li>
<li><code>listen</code>(target: ‘window’ | ‘document’ | ‘body’ | any, eventName: string, callback: (event: any) =&gt; boolean | void): () =&gt; void</li>
</ul>
]]></content>
      
        
        <tags>
            
            <tag> Angular </tag>
            
        </tags>
        
    </entry>
    
    <entry>
      <title><![CDATA[Directive]]></title>
      <url>/2017/10/30/Directive/</url>
      <content type="html"><![CDATA[<h2 id="Directive-4-16-2017-4-40-14-PM"><a href="#Directive-4-16-2017-4-40-14-PM" class="headerlink" title="Directive 4/16/2017 4:40:14 PM "></a>Directive <span style="float:right;font-size:12px;line-height:40px;">4/16/2017 4:40:14 PM </span></h2><blockquote>
<p><strong>what it does</strong></p>
</blockquote>
<p>Marks a class as an Angular directive and collects directive configuration metadata.</p>
<blockquote>
<p><strong>how to use</strong></p>
</blockquote>
<pre><code>import { Directive } from &apos;@angular/core&apos;;
@Directive({
    selector?: string,
    inputs  ?: string[],
    outputs ?: string[],
    host    ?: {
        [key: string]: string
    },
    providers?: Provider[],
    exportAs ?: string,
    queries  ?: {
        [key: string]: any
    }
})
export class MyDirective{
}
</code></pre><blockquote>
<p><strong>description</strong></p>
</blockquote>
<p>A directive must belong to an NgModule in order for it to be usable by another directive, component, or application. To specify that a directive is a member of an NgModule.</p>
<p>you should list it in the <code>declarations</code> field of that NgModule.</p>
<blockquote>
<p><strong>metadata properties</strong></p>
</blockquote>
<p><code>exportAs</code> - name under which the component  instance is exported in a template</p>
<p><code>host</code> - map of class property to host element bindings for events, properties and attributes</p>
<p><code>inputs</code> - list of class property names to data-bind as component inputs</p>
<p><code>outputs</code> - list of class property names that expose output events that others can subscribe to</p>
<p><code>providers</code> - list of providers available to this component and its children</p>
<p><code>queries</code> -  configure queries that can be injected into the component</p>
<p><code>selector</code> - css selector that identifies this component in a template</p>
<blockquote>
<p><strong>selector</strong> </p>
</blockquote>
<p>may be declared as one of the following:</p>
<ul>
<li><code>element-name</code>: select by element name.</li>
<li><code>.class</code>: select by class name.</li>
<li><code>[attribute]</code>: select by attribute name.</li>
<li><code>[attribute=value]</code>: select by attribute name and value.</li>
<li><code>:not(sub_selector)</code>: select only if the element does not match the <code>sub_selector</code>.</li>
<li><code>selector1, selector2</code>: select if either <code>selector1</code> or <code>selector2</code> matches.</li>
</ul>
<blockquote>
<p><strong>inputs</strong></p>
</blockquote>
<p>Enumerates the set of data-bound input properties for a directive</p>
<p>The <code>inputs</code> property defines a set of <code>directiveProperty</code> to <code>bindingProperty</code> configuration:</p>
<ul>
<li><code>directiveProperty</code> specifies the component property where the value is written.</li>
<li><code>bindingProperty</code> specifies the DOM property where the value is read from.</li>
</ul>
<p>When <code>bindingProperty</code> is not provided, it is assumed to be equal to <code>directiveProperty</code>.</p>
<blockquote>
<p><strong>outputs</strong> </p>
</blockquote>
<p>Enumerates the set of event-bound output properties.</p>
<p>When an output property emits an event, an event handler attached to that event the template is invoked.</p>
<p><img src="https://github.com/Leooman/Angular/raw/master/images/output.jpg" alt="output"></p>
<blockquote>
<p><strong>host</strong></p>
</blockquote>
<ul>
<li><strong>host listeners</strong>:</li>
</ul>
<p>Specify the events, actions, properties and attributes related to the host element.</p>
<p>Specifies which DOM events a directive listens to via a set of <code>(event)</code> to <code>method</code></p>
<p>key-value pairs:</p>
<ul>
<li><code>event</code>: the DOM event that the directive listens to.</li>
<li><code>statement</code>: the statement to execute when the event occurs.</li>
</ul>
<p>If the evaluation of the statement returns <code>false</code>, then <code>preventDefault</code>is applied on the DOM event.</p>
<p><img src="https://github.com/Leooman/Angular/raw/master/images/host_listener.jpg" alt="host listeners"></p>
<ul>
<li><strong>host property binding</strong></li>
</ul>
<p>Specifies which DOM properties a directive updates.</p>
<ul>
<li><strong>host attributes binding</strong></li>
</ul>
<p>Specifies static attributes that should be propagated to a host element.</p>
<p><img src="https://github.com/Leooman/Angular/raw/master/images/host_attr.jpg" alt="host attributes"></p>
<blockquote>
<p><strong>providers</strong></p>
</blockquote>
<p><img src="https://github.com/Leooman/Angular/raw/master/images/providers.jpg" alt="providers"></p>
<blockquote>
<p><strong>exportAs</strong></p>
</blockquote>
<p>Defines the name that can be used in the template to assign this directive to a variable.</p>
<p><img src="https://github.com/Leooman/Angular/raw/master/images/exportas.jpg" alt="exportAs"></p>
<blockquote>
<p><strong>queries</strong></p>
</blockquote>
<p>Configures the queries that will be injected into the directive.</p>
<ul>
<li><p>Content queries are set before the <code>ngAfterContentInit</code> callback is called.</p>
</li>
<li><p>View queries are set before the <code>ngAfterViewInit</code> callback is called.</p>
</li>
</ul>
]]></content>
      
        
        <tags>
            
            <tag> Angular </tag>
            
        </tags>
        
    </entry>
    
    <entry>
      <title><![CDATA[Communication]]></title>
      <url>/2017/10/30/Communication/</url>
      <content type="html"><![CDATA[<h2 id="Component-Communication-4-23-2017-8-58-19-AM"><a href="#Component-Communication-4-23-2017-8-58-19-AM" class="headerlink" title="Component Communication 4/23/2017 8:58:19 AM "></a>Component Communication <span style="float:right;font-size:12px;line-height:40px;">4/23/2017 8:58:19 AM </span></h2><blockquote>
<p><strong>Input</strong></p>
</blockquote>
<pre><code>@Component({
    selector: &apos;bank-account&apos;,
    template: `
      Bank Name: {{bankName}}
      Account Id: {{id}}
    `
  })
  class BankAccount {
    @Input() bankName: string;
    @Input(&apos;account-id&apos;) id: string;

    // this property is not bound, and won&apos;t be automatically updated by Angular
    normalizedBankName: string;
  }

  @Component({
    selector: &apos;app&apos;,
    template: `
      &lt;bank-account bank-name=&quot;RBC&quot; account-id=&quot;4747&quot;&gt;&lt;/bank-account&gt;
    `
  })

  class App {}
</code></pre><blockquote>
<p><strong>Output</strong></p>
</blockquote>
<pre><code>@Directive({
    selector: &apos;interval-dir&apos;,
  })
  class IntervalDir {
    @Output() everySecond = new EventEmitter();
    @Output(&apos;everyFiveSeconds&apos;) five5Secs = new EventEmitter();

    constructor() {
      setInterval(() =&gt; this.everySecond.emit(&quot;event&quot;), 1000);
      setInterval(() =&gt; this.five5Secs.emit(&quot;event&quot;), 5000);
    }
  }

  @Component({
    selector: &apos;app&apos;,
    template: `
      &lt;interval-dir (everySecond)=&quot;everySecond()&quot; (everyFiveSeconds)=&quot;everyFiveSeconds()&quot;&gt;
      &lt;/interval-dir&gt;
    `
  })
  class App {
    everySecond() { console.log(&apos;second&apos;); }
    everyFiveSeconds() { console.log(&apos;five seconds&apos;); }
  }
</code></pre><blockquote>
<p><strong>HostBinding</strong></p>
</blockquote>
<pre><code>@Directive({selector: &apos;[ngModel]&apos;})
  class NgModelStatus {
    constructor(public control:NgModel) {}
    @HostBinding(&apos;class.valid&apos;) get valid() { return this.control.valid; }
    @HostBinding(&apos;class.invalid&apos;) get invalid() { return this.control.invalid; }
  }

  @Component({
    selector: &apos;app&apos;,
    template: `&lt;input [(ngModel)]=&quot;prop&quot;&gt;`,
  })
  class App {
    prop;
  }
</code></pre><blockquote>
<p><strong>HostListener</strong></p>
</blockquote>
<pre><code>@Directive({selector: &apos;button[counting]&apos;})
  class CountClicks {
    numberOfClicks = 0;

    @HostListener(&apos;click&apos;, [&apos;$event.target&apos;])
    onClick(btn) {
      console.log(&apos;button&apos;, btn, &apos;number of clicks:&apos;, this.numberOfClicks++);
    }
  }

  @Component({
    selector: &apos;app&apos;,
    template: &apos;&lt;button counting&gt;Increment&lt;/button&gt;&apos;,
  })
  class App {}
</code></pre>]]></content>
      
        
        <tags>
            
            <tag> Angular </tag>
            
        </tags>
        
    </entry>
    
    <entry>
      <title><![CDATA[Component]]></title>
      <url>/2017/10/30/Component/</url>
      <content type="html"><![CDATA[<h2 id="Component-extends-Directive-4-16-2017-5-36-18-PM"><a href="#Component-extends-Directive-4-16-2017-5-36-18-PM" class="headerlink" title="Component extends Directive 4/16/2017 5:36:18 PM "></a>Component extends Directive <span style="float:right;font-size:12px;line-height:40px;">4/16/2017 5:36:18 PM </span></h2><blockquote>
<p><strong>what it does</strong></p>
</blockquote>
<p>Marks a class as an Angular component and collects component configuration<br>metadata.</p>
<blockquote>
<p><strong>how to use</strong></p>
</blockquote>
<pre><code>import { Component } from &apos;@angular/core&apos;;
@Component({
    changeDetection?: ChangeDetectionStrategy,
    viewProviders?: Provider[],
    moduleId?: string,
    templateUrl?: string,
    template?: string,
    styleUrls?: string[],
    styles?: string[],
    animations?: AnimationEntryMetadata[],
    encapsulation?: ViewEncapsulation,
    interpolation?: [string, string],
    entryComponents?: Array&lt;Type&lt;any&gt; | any[]&gt;
})
export class MyComponent{
}
</code></pre><blockquote>
<p><strong>description</strong></p>
</blockquote>
<p>A component must belong to an NgModule in order for it to be usable by another component or application. To specify that a component is a member of an NgModule</p>
<p>you should list it in the <code>declarations</code> field of that NgModule.</p>
<blockquote>
<p><strong>metadata properties</strong></p>
</blockquote>
<p><code>animations</code> - list of animations of this component</p>
<p><code>changeDetection</code> - change detection strategy used by this component</p>
<p><code>encapsulation</code> - style encapsulation strategy used by this component</p>
<p><code>entryComponents</code> - list of components that are dynamically inserted into the view of this component</p>
<p><code>exportAs</code> - name under which the component instance is exported in a template</p>
<p><code>host</code> - map of class property to host element bindings for events, properties and attributes</p>
<p><code>inputs</code> - list of class property names to data-bind as component inputs</p>
<p><code>interpolation</code> - custom interpolation markers used in this component’s template</p>
<p><code>moduleId</code> - ES/CommonJS module id of the file in which this component is defined</p>
<p><code>outputs</code> - list of class property names that expose output events that others can subscribe to</p>
<p><code>providers</code> - list of providers available to this component and its children</p>
<p><code>queries</code> -  configure queries that can be injected into the component</p>
<p><code>selector</code> - css selector that identifies this component in a template</p>
<p><code>styleUrls</code> - list of urls to stylesheets to be applied to this component’s view</p>
<p><code>styles</code> - inline-defined styles to be applied to this component’s view</p>
<p><code>template</code> - inline-defined template for the view</p>
<p><code>templateUrl</code> - url to an external file containing a template for the view</p>
<p><code>viewProviders</code> - list of providers available to this component and its view children</p>
<blockquote>
<p><strong>changeDetection</strong></p>
</blockquote>
<p>The <code>changeDetection</code> property defines, whether the change detection will be checked every time or only when the component tells it to do so.</p>
<p><img src="https://github.com/Leooman/Angular/raw/master/images/changedetection.jpg" alt="changeDetection"></p>
<blockquote>
<p><strong>interpolation</strong></p>
</blockquote>
<p>Overrides the default encapsulation start and end delimiters (respectively <code></code>)</p>
<p><img src="https://github.com/Leooman/Angular/raw/master/images/interpolation.jpg" alt="interpolation"></p>
<blockquote>
<p><strong>viewProviders</strong></p>
</blockquote>
<p>Defines the set of injectable objects that are visible to its view DOM children.</p>
<p><img src="https://github.com/Leooman/Angular/raw/master/images/viewproviders.jpg" alt="viewProviders"></p>
<blockquote>
<p><strong>moduleId</strong></p>
</blockquote>
<p>Needed to be able to resolve relative urls for templates and styles.</p>
<p>In CommonJS, this can always be set to <code>module.id</code>, similarly SystemJS exposes <code>__moduleName</code> variable within each module.</p>
<pre><code>@Directive({
  selector: &apos;someDir&apos;,
  moduleId: module.id
})
class SomeDir {
}
</code></pre><blockquote>
<p><strong>templateUrl</strong></p>
</blockquote>
<p>Only one of <code>templateUrl</code> or <code>template</code> can be defined per View.</p>
<blockquote>
<p><strong>template</strong></p>
</blockquote>
<p>Only one of <code>templateUrl</code> or <code>template</code> can be defined per Component.</p>
<blockquote>
<p><strong>styleUrls</strong></p>
</blockquote>
<p>Specifies stylesheet URLs for an Angular component.</p>
<blockquote>
<p><strong>styles</strong></p>
</blockquote>
<p>Specifies inline stylesheets for an Angular component.</p>
<blockquote>
<p><strong>entryComponents</strong></p>
</blockquote>
<p>Defines the components that should be compiled as well when this component is defined. For each components listed here, Angular will create a {@link ComponentFactory} and store it in the {@link ComponentFactoryResolver}.</p>
<blockquote>
<p><strong>encapsulation</strong></p>
</blockquote>
<p>Specifies how the template and the styles should be encapsulated:</p>
<ul>
<li><p>{@link ViewEncapsulation#Native <code>ViewEncapsulation.Native</code>} to use shadow roots ,only works if natively available on the platform,</p>
</li>
<li><p>{@link ViewEncapsulation#Emulated <code>ViewEncapsulation.Emulated</code>} to use shimmed CSS that emulates the native behavior,</p>
</li>
<li><p>{@link ViewEncapsulation#None <code>ViewEncapsulation.None</code>} to use global CSS without any encapsulation.</p>
</li>
</ul>
<p>When no <code>encapsulation</code> is defined for the component, the default value from the {@link CompilerConfig} is used. The default is <code>ViewEncapsulation.Emulated</code>. Provide a new <code>CompilerConfig</code> to override this value.</p>
<p>If the encapsulation is set to <code>ViewEncapsulation.Emulated</code> and the component has no <code>styles</code> nor <code>styleUrls</code> the encapsulation will automatically be switched to <code>ViewEncapsulation.None</code>.</p>
<blockquote>
<p><strong>animations</strong></p>
</blockquote>
<h3 id="DSL-Animation-Functions"><a href="#DSL-Animation-Functions" class="headerlink" title="DSL Animation Functions"></a>DSL Animation Functions</h3><ul>
<li>{@link trigger trigger()}</li>
<li>{@link state state()}</li>
<li>{@link transition transition()}</li>
<li>{@link group group()}</li>
<li>{@link sequence sequence()}</li>
<li>{@link style style()}</li>
<li>{@link animate animate()}</li>
<li>{@link keyframes keyframes()}</li>
</ul>
<p>For animations to be available for use, animation state changes are placed within {@link trigger animation triggers} which are housed inside of the <code>animations</code> annotation metadata. Within a trigger both {@link state state} and {@link transition transition} entries can be placed.</p>
<pre><code>@Component({
  selector: &apos;animation-cmp&apos;,
  templateUrl: &apos;animation-cmp.html&apos;,
  animations: [
    // this here is our animation trigger that
    // will contain our state change animations.
    trigger(&apos;myTriggerName&apos;, [
      // the styles defined for the `on` and `off`
      // states declared below are persisted on the
      // element once the animation completes.
      state(&apos;on&apos;, style({ opacity: 1 }),
      state(&apos;off&apos;, style({ opacity: 0 }),

      // this here is our animation that kicks off when
      // this state change jump is true
      transition(&apos;on =&gt; off&apos;, [
        animate(&quot;1s&quot;)
      ])
    ])
  ]
})
</code></pre><p>When a trigger is created then it can be bound onto an element within the component’s template via a property prefixed by an <code>@</code> symbol followed by trigger name and an expression that is used to determine the state value for that trigger.</p>
<pre><code>&lt;div @myTriggerName=&quot;expression&quot;&gt;...&lt;/div&gt;
</code></pre><h3 id="Animation-Example"><a href="#Animation-Example" class="headerlink" title="Animation Example"></a>Animation Example</h3><p><img src="https://github.com/Leooman/Angular/raw/master/images/fadein.jpg" alt="fade-in"></p>
<p><img src="https://github.com/Leooman/Angular/raw/master/images/flyin.jpg" alt="fly-in"></p>
]]></content>
      
        
        <tags>
            
            <tag> Angular </tag>
            
        </tags>
        
    </entry>
    
  
  
</search>
