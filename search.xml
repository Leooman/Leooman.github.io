<?xml version="1.0" encoding="utf-8"?>
<search> 
  
    
    <entry>
      <title><![CDATA[变更检测]]></title>
      <url>/2018/06/18/changeDetection/</url>
      <content type="html"><![CDATA[<h2 id="change-detector变化检测器"><a href="#change-detector变化检测器" class="headerlink" title="change detector变化检测器"></a>change detector变化检测器</h2><ul>
<li>触发时机：基本上所有的异步事件发生（并且回调函数已经执行完毕）以后，都需要触发change detection（因为此时进程的状态可能已经发生改变），如Events ，XHR ，Timers </li>
<li>单向数据流：沿着组件树进行变化检测，检查完父组件以后再检查子组件，在检查父组件的时候可能会更新子组件中的绑定，但是在检查子组件的时候（此时父组件已经检查完毕）不会更新父组件的数据，只需要执行一次Change Detection，就能使得view与组件中的数据一致。在AngularJS中，由于在检查一个组件的时候可能会改动另一个组件中的数据，因此需要多次检查，直到数据“稳定”下来<br><img src="https://github.com/Leooman/NgAdmin/raw/gh-pages/images/change_detection.png" alt="change_detection"></li>
<li>专用change detector：每个组件都有一个自己的change detector(Angular compiler为每个component编译生成专门检测它的view的代码)，这使得每个change detector的检测代码非常地简单高效</li>
</ul>
<p><em>多态(polymorphic)：通用性往往意味着多态的存在。多态的含义是，一段代码，多次执行，但是每次执行所操作的对象都不是同一个对象，甚至这些对象的shape相差巨大，这种代码难以被优化。大量使用单态（monomorphic）是Angular速度爆炸的重要原因！除了Change Detection方面，Angular在其他地方也使用了单态的优化思想</em></p>
<h2 id="ChangeDetectionStrategy-变更检测策略"><a href="#ChangeDetectionStrategy-变更检测策略" class="headerlink" title="ChangeDetectionStrategy 变更检测策略"></a>ChangeDetectionStrategy 变更检测策略</h2><ul>
<li><code>OnPush = 0</code>：若输入属性没有发生变化，组件的变化检测将会被跳过</li>
<li><code>Default = 1</code>：组件的变更检测一直执行，默认采用此策略，因为即使@Input()对象的引用没有变，其中的属性可能已经发生变化（JavaScript对象的动态性），变化检测需要将这种变化也反映在view和子组件上</li>
</ul>
<h2 id="ChangeDetectorStatus-变更检测状态"><a href="#ChangeDetectorStatus-变更检测状态" class="headerlink" title="ChangeDetectorStatus 变更检测状态"></a>ChangeDetectorStatus 变更检测状态</h2><ul>
<li><code>CheckOnce = 0</code>：检测变化后变更检测器模式切换为<code>Checked</code></li>
<li><code>Checked = 1</code>：变更检测会被跳过</li>
<li><code>CheckAlways = 2</code>：检测变化后变更检测器模式保持<code>CheckAlways</code></li>
<li><code>Detached = 3</code>：已将组件从变更检测树中分离</li>
<li><code>Errored = 4</code>：绑定检查报错或调用生命周期方法与当前状态不一致，变更检测停止工作</li>
<li><code>Destroyed = 5</code>：变更检测器销毁</li>
</ul>
<h2 id="ChangeDetectorRef-变更检测机制"><a href="#ChangeDetectorRef-变更检测机制" class="headerlink" title="ChangeDetectorRef 变更检测机制"></a>ChangeDetectorRef 变更检测机制</h2><pre><code>export declare abstract class ChangeDetectorRef {
  markForCheck(): void;
  detach(): void;
  detectChanges(): void;
  checkNoChanges(): void;
  reattach(): void;
}
</code></pre><ul>
<li><code>markForCheck(): void</code>：标记为脏状态进入变更检测周期的检测</li>
</ul>
<pre><code>//=&gt; OnPush策略下的组件会进入checked状态，想要进行变更检测要调用markForCheck()
@Component({
  selector: &apos;my-app&apos;,
  template: `Number of ticks: {{numberOfTicks}}`
  changeDetection: ChangeDetectionStrategy.OnPush,
})
class AppComponent {
  numberOfTicks = 0;
  constructor(private ref: ChangeDetectorRef) {
    setInterval(() =&gt; {
      this.numberOfTicks++;
      this.ref.markForCheck();
    }, 1000);
  }
}
</code></pre><ul>
<li><code>detach(): void</code>：将视图从变更检测树中分离不进行检测</li>
<li><code>detectChanges(): void</code>：该组件到各个子组件执行一次变化检测，可用于自主控制视图更新频率 </li>
</ul>
<pre><code>//=&gt; 将组件从变更检测树中分离出去，实时变化的数据改变为每隔5s触发一次变更检测
//=&gt; data.service.ts
export class DataService {
  private arr:number[];
  get data() {
      let array:numner[];
      array=this.arr=this.arr ? this.arr.map(x=&gt;x+=5) : [1,2,3,4,5];
      return array;
  }
}

//=&gt; giantList.component.ts
@Component({
  selector: &apos;giant-list&apos;,
  template: `
    &lt;li *ngFor=&quot;let d of dataProvider.data&quot;&gt;Data {{d}}&lt;/li&gt;
  `,
})
export class GiantListComponent {
  constructor(private ref: ChangeDetectorRef, private dataProvider: DataService) {
    ref.detach();
    setInterval(() =&gt; {
      this.ref.detectChanges();
    }, 5000);
  }
}

//=&gt; app.component.ts
@Component({
  selector: &apos;app&apos;,
  providers: [DataService],
  template: `
    &lt;giant-list&gt;&lt;giant-list&gt;
  `,
})
class App {
}
</code></pre><ul>
<li><code>reattach(): void</code>：将视图重新附加到变更检测树中进入检测</li>
</ul>
<pre><code>//=&gt; 切换视图在变更检测树中的分离或附加状态
//=&gt; data.service.ts
export class DataService {
  data = 1;
  constructor() {
    setInterval(() =&gt; {
      this.data = this.data * 2;
    }, 500);
  }
}

//=&gt; liveData.component.ts
@Component({
  selector: &apos;live-data&apos;,
  inputs: [&apos;live&apos;],
  template: &apos;Data: {{dataProvider.data}}&apos;
})
export class LiveDataComponent {
  constructor(private ref: ChangeDetectorRef, private dataProvider: DataService) {}
  set live(value:boolean) {
    value ? this.ref.reattach() : this.ref.detach()
  }
}

//=&gt; app.component.ts
@Component({
  selector: &apos;my-app&apos;,
  providers: [DataProvider],
  template: `
    Live Update: &lt;input type=&quot;checkbox&quot; [(ngModel)]=&quot;live&quot;&gt;
    &lt;live-data [live]=&quot;live&quot;&gt;&lt;live-data&gt;
  `,
})
export class AppComponent {
  live = true;
}
</code></pre><ul>
<li><code>checkNoChanges(): void</code>：检测组件及其子组件，存在变化则报错。用于开发模式确保变化检测期间没有引进其他变化</li>
</ul>
]]></content>
      
        
        <tags>
            
            <tag> Angular6.0 </tag>
            
        </tags>
        
    </entry>
    
    <entry>
      <title><![CDATA[函数式编程]]></title>
      <url>/2018/05/23/functionProgram/</url>
      <content type="html"><![CDATA[<h2 id="面向对象编程"><a href="#面向对象编程" class="headerlink" title="面向对象编程"></a>面向对象编程</h2><p>在面向对象编程中，面向对象程序设计（英语：Object-oriented programming，缩写：OOP）是种具有对象概念的程序编程范型，同时也是一种程序开发的方法。它可能包含数据、属性、代码与方法。对象则指的是类的实例。</p>
<p>在面向对象程序编程里，计算机程序会被设计成彼此相关的对象 ，将对象作为程序的基本单元。面向对象程序设计中的每一个对象都应该能够接受数据、处理数据并将数据传达给其它对象。</p>
<p>目前面向对象程序设计推广了程序的灵活性和可维护性，并且在大型项目设计中广为应用。能够让人们更简单地设计并维护程序，使得程序更加便于分析、设计、理解。同时它也是易拓展的，由于继承、封装、多态的特性，自然设计出高内聚、低耦合的系统结构，使得系统更灵活、更容易扩展，而且成本较低。</p>
<p>在面向对象编程的基础上发展出来的23种设计模式广泛应用于现今的软件工程中，极大方便了代码的书写与维护。</p>
<p>创建型模式，共五种：工厂方法模式、抽象工厂模式、单例模式、建造者模式、原型模式。</p>
<p>结构型模式，共七种：适配器模式、装饰器模式、代理模式、外观模式、桥接模式、组合模式、享元模式。</p>
<p>行为型模式，共十一种：策略模式、模板方法模式、观察者模式、迭代子模式、责任链模式、命令模式、备忘录模式、状态模式、访问者模式、中介者模式、解释器模式。</p>
<p>面向对象编程以数据为核心，所以在多线程并发编程中，多个线程同时操作数据的时候可能会导致数据修改的不确定性。 </p>
<h2 id="函数式编程"><a href="#函数式编程" class="headerlink" title="函数式编程"></a>函数式编程</h2><p>函数式编程（Functional programming）是与面向对象编程（Object-oriented programming）并列的编程范式。</p>
<p>函数式编程就是一种抽象程度很高的编程范式，纯粹的函数式编程语言编写的函数<a href="http://www.ruanyifeng.com/blog/2017/03/pointfree.html" target="_blank" rel="noopener">Pointfree编程风格</a>没有变量，因此，任意一个函数，只要输入是确定的，输出就是确定的，这种纯函数我们称之为没有副作用。而允许使用变量的程序设计语言，由于函数内部的变量状态不确定，同样的输入，可能得到不同的输出，因此，这种函数是有副作用的。 </p>
<p>对象与对象之间的关系是面向对象编程首要考虑的问题，而在函数式编程中，所有的数据都是不可变的，不同的函数之间通过数据流来交换信息，<code>函数作为FP中的一等公民</code>，享有跟数据一样的地位，可以作为参数传递给下一个函数，同时也可以作为返回值。 </p>
<p>在函数式编程中，由于数据全部都是不可变的，所以没有并发编程的问题，是多线程安全的。可以有效降低程序运行中所产生的副作用，对于快速迭代的项目来说，函数式编程可以实现函数与函数之间的热切换而不用担心数据的问题，因为它是以函数作为最小单位的，只要函数与函数之间的关系正确即可保证结果的正确性。</p>
<h2 id="Why-Ramda"><a href="#Why-Ramda" class="headerlink" title="Why Ramda"></a>Why Ramda</h2><p>Ramda强调一种更纯粹的函数<code>Pure function</code>风格，不可变和无副作用是其设计的核心，当给予function相同的参数，永远返回相同的值，并且没有显著的副作用<code>side effect</code>。</p>
<figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//side effect指一个function做了跟本身运算返回值没有关系的事，比如发送`http request`请求或修改某个全局变量或传入参数的值，甚至是执行`console.log`</span></span><br><span class="line"></span><br><span class="line"><span class="comment">//slice不管执行多少次，返回值都是相同的，并且除了返回value值外，没有做任何事</span></span><br><span class="line"><span class="keyword">var</span> arr = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>];</span><br><span class="line">arr.slice(<span class="number">0</span>, <span class="number">3</span>); <span class="comment">// [1, 2, 3]</span></span><br><span class="line">arr.slice(<span class="number">0</span>, <span class="number">3</span>); <span class="comment">// [1, 2, 3]</span></span><br><span class="line">arr.slice(<span class="number">0</span>, <span class="number">3</span>); <span class="comment">// [1, 2, 3]</span></span><br><span class="line"></span><br><span class="line"><span class="comment">//splice没执行一次就影响arr的值，导致每次结果都不同，不符合pure function不可变的原则</span></span><br><span class="line"><span class="keyword">var</span> arr = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>];</span><br><span class="line">arr.splice(<span class="number">0</span>, <span class="number">3</span>); <span class="comment">// [1, 2, 3]</span></span><br><span class="line">arr.splice(<span class="number">0</span>, <span class="number">3</span>); <span class="comment">// [4, 5]</span></span><br><span class="line">arr.slice(<span class="number">0</span>, <span class="number">3</span>); <span class="comment">// []</span></span><br></pre></td></tr></table></figure>
<p>Ramda所有函数都是自动柯里化的。</p>
<figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">R.add(<span class="number">2</span>, <span class="number">3</span>);       <span class="comment">//=&gt;  5</span></span><br><span class="line">R.add(<span class="number">7</span>)(<span class="number">10</span>);      <span class="comment">//=&gt; 17</span></span><br></pre></td></tr></table></figure>
<p>Ramda中数据都是作为最后一个参数传入的。</p>
<figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">var</span> R = <span class="built_in">require</span>(<span class="string">'ramda'</span>);</span><br><span class="line">R.map(square, [<span class="number">4</span>, <span class="number">8</span>]) <span class="comment">// [16, 64]</span></span><br></pre></td></tr></table></figure>
<p>不同于underscore和lodash：</p>
<figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">var</span> square = <span class="function"><span class="params">n</span> =&gt;</span> n * n;</span><br><span class="line">_.map([<span class="number">4</span>, <span class="number">8</span>], square) <span class="comment">// [16, 64]</span></span><br></pre></td></tr></table></figure>]]></content>
      
        
        <tags>
            
            <tag> Ramda </tag>
            
        </tags>
        
    </entry>
    
    <entry>
      <title><![CDATA[组件]]></title>
      <url>/2018/05/12/component/</url>
      <content type="html"><![CDATA[<h2 id="description"><a href="#description" class="headerlink" title="description"></a>description</h2><p>组件是Angular应用程序中UI的最基本构建块。</p>
<p><code>component</code>是对<code>directive</code>的扩展，不同于<code>directive</code>的是，<code>component</code>始终具有模板<code>template</code>并且每个模板中的元素只能实例化一个组件。</p>
<p>一个组件必须属于<code>NgModule</code>模块，在<code>NgModule</code>的<code>declarations</code>字段中声明，以便其他的组件或应用使用。</p>
<h2 id="how-to-use"><a href="#how-to-use" class="headerlink" title="how to use"></a>how to use</h2><pre><code>import { Component } from &apos;@angular/core&apos;;
@Component({
    changeDetection?: ChangeDetectionStrategy,     //=&gt; 定义使用的变更检测策略
    viewProviders?: Provider[],                    //=&gt; 设置组件或子组件可以使用的服务
    moduleId?: string,                             //=&gt; 包含该组件的模块的模块ID，用于解析模板和样式的相对路径
    templateUrl?: string,                          //=&gt; 外部模板文件地址
    template?: string,                             //=&gt; 内联模板
    styleUrls?: string[],                          //=&gt; 外部样式文件地址
    styles?: string[],                             //=&gt; 内联样式
    animations?: AnimationEntryMetadata[],         //=&gt; 定义动画
    encapsulation?: ViewEncapsulation,             //=&gt; 指定模板和样式封装规则
    interpolation?: [string, string],              //=&gt; 重写默认的插值运算符（默认为&apos;{{'和'}}&apos;）
    entryComponents?: Array&lt;Type&lt;any&gt; | any[]&gt;,    //=&gt; 组件定义时需要预编译的组件
    preserveWhitespaces?: boolean                  //=&gt; 是否保留编译后的空白字符
})
export class MyComponent{
}
</code></pre><h2 id="changeDetection"><a href="#changeDetection" class="headerlink" title="changeDetection"></a>changeDetection</h2><p><em>组件实例化时创建一个变化检测器，用来检测组件的绑定，<a href="#docs/Change_detection">changeDetection</a>属性定义了变化检测方式</em></p>
<pre><code>@Component({
    selector:&apos;cmp&apos;,
    changeDetection:ChangeDetectionStrategy.OnPush || 0,
    template:`Number of ticks: {{numberOfTicks}}`
})

export class CmpComponent{
    numberOfTicks = 0
    constructor() {
        setInterval(() =&gt; {
            this.numberOfTicks ++
        },1000)
    }
}
</code></pre><h2 id="viewProviders"><a href="#viewProviders" class="headerlink" title="viewProviders"></a>viewProviders</h2><pre><code>//=&gt; role.directive.ts
@Directive({
    selector:&apos;greet&apos;,
    exportAs:&apos;G&apos;
})
export class GreetDirective{
    greet:GreetService;
    constructor(greeter:GreetService){
        this.greeter = greeter;
    }
}
//=&gt; greet.service.ts
import { Injectable } from &apos;@angular/core&apos;
@Injectable()
export class GreetService{
    constructor(){}
    greet(name:string){
        return &quot;hello&quot;+name;
    }
}
//=&gt; app.component.ts
@Component({
    selector:&apos;my-app&apos;,
    templateUrl:&apos;./app.component.html&apos;,
    viewProviders:[
        GreetService
    ]
})
//=&gt; app.component.html
&lt;greet #g=&apos;G&apos;&gt;{{g.greeter.greet('Angular')}}&lt;/greet&gt;
</code></pre><h2 id="moduleId"><a href="#moduleId" class="headerlink" title="moduleId"></a>moduleId</h2><p><em>解决模板与样式文件的相对路径问题，可以设置为<code>module.id</code></em> </p>
<pre><code>@Directive({
  selector: &apos;someDir&apos;,
  moduleId: module.id
})
class SomeDir {
}
</code></pre><h2 id="templateUrl"><a href="#templateUrl" class="headerlink" title="templateUrl"></a>templateUrl</h2><p><em>指定组件的模板文件地址，每个组件只可以定义<code>templateUrl</code>或<code>template</code>中的一个，之后定义的会覆盖之前定义的</em></p>
<pre><code>@Component({
    selector:&apos;my-app&apos;,
    templateUrl:&apos;./app.component.html&apos;,      //=&gt; 视图不渲染
    template:`&lt;p&gt;Angular&lt;/p&gt;`                //=&gt; 视图渲染
})
</code></pre><h2 id="template"><a href="#template" class="headerlink" title="template"></a>template</h2><p><em>指定组件的内联模板，每个组件只可以定义<code>templateUrl</code>或<code>template</code>中的一个，之后定义的会覆盖之前定义的</em></p>
<h2 id="styleUrls"><a href="#styleUrls" class="headerlink" title="styleUrls"></a>styleUrls</h2><p><em>指定样式文件地址的列表集合</em></p>
<pre><code>@Component({
    selector:&apos;my-app&apos;,
    templateUrl:&apos;./app.component.html&apos;,
    styleUrls:[&apos;./app.component.css&apos;,&apos;./css/demo.css&apos;]
})
</code></pre><h2 id="styles"><a href="#styles" class="headerlink" title="styles"></a>styles</h2><p><em>指定组件的内联样式</em></p>
<pre><code>@Component({
    selector:&apos;my-app&apos;,
    templateUrl:&apos;./app.component.html&apos;,
    styles:[`p{background:#f00;color:#fff;}`]
})
</code></pre><h2 id="encapsulation"><a href="#encapsulation" class="headerlink" title="encapsulation"></a>encapsulation</h2><p>定义了组件中的模板和样式该如何封装：</p>
<ul>
<li><code>ViewEncapsulation.Emulated</code> ：仿真模式，无 Shadow DOM， 通过Angular来模拟类似Shadow DOM的行为 ，使样式不受外部影响</li>
</ul>
<pre><code>@Component({
  template: `&lt;h1&gt;test&lt;/h1&gt;`,
  styles: [`h1 { color: #f50; }`],
  encapsulation: ViewEncapsulation.Emulated || 0
})
//=&gt;  &lt;style&gt;h1[_ngcontent-c0] { color: #f50; }&lt;/style&gt;
    &lt;h1 _ngcontent-c0&gt;test&lt;/h1&gt;
</code></pre><ul>
<li><code>ViewEncapsulation.Native</code> ：使用shadow roots影子根（如果浏览器支持<a href="https://developer.mozilla.org/zh-CN/docs/Web/Web_Components/%E5%BD%B1%E5%AD%90_DOM" target="_blank" rel="noopener">Shadow-DOM</a>）</li>
</ul>
<pre><code>@Component({
  template: `&lt;h1&gt;test&lt;/h1&gt;`,
  styles: [`h1 { color: #f50; }`],
  encapsulation: ViewEncapsulation.Native || 1
})
//=&gt;  #shadow-root (open)
        &lt;style&gt;h1 { color: #f50; }&lt;/style&gt;
        &lt;h1&gt;test&lt;/h1&gt;
</code></pre><ul>
<li><code>ViewEncapsulation.None</code> ：无任何封装行为，作为全局样式</li>
</ul>
<pre><code>@Component({
  template: `&lt;h1&gt;test&lt;/h1&gt;`,
  styles: [`h1 { color: #f50; }`],
  encapsulation: ViewEncapsulation.None || 2
})
//=&gt;  &lt;style&gt;h1 { color: #f50; }&lt;/style&gt;
      &lt;h1&gt;test&lt;/h1&gt;
</code></pre><p><em>当<code>encapsulation</code>在组件中没有被定义时使用<code>CompilerOptions</code>里定义的默认值<code>ViewEncapsulation.Emulated</code>,并提供一个<code>CompilerOptions</code>来重写此值</em></p>
<p><em>如果<code>encapsulation</code>设置为<code>ViewEncapsulation.Emulated</code>并且组件中没有定义<code>styles</code>或者<code>styleUrls</code>,<code>encapsulation</code>会自动切换为<code>ViewEncapsulation.None</code></em></p>
<h2 id="interpolation"><a href="#interpolation" class="headerlink" title="interpolation"></a>interpolation</h2><pre><code>@Component({
    selector:&apos;my-app&apos;,
    template:`&lt;p&gt;Number of ticks: $$ numberOfTicks $&lt;/p&gt;`,
    interpolation:[&quot;$$&quot;,&quot;$&quot;]
})
</code></pre><h2 id="entryComponents"><a href="#entryComponents" class="headerlink" title="entryComponents"></a>entryComponents</h2><p><em>指定组件定义时需要编译的组件列表，对于列表中的每个组件，Angular会创建一个<code>ComponentFactory</code>并且保存在<code>ComponentFactoryResolver</code>中</em></p>
<h2 id="preserveWhitespaces"><a href="#preserveWhitespaces" class="headerlink" title="preserveWhitespaces"></a>preserveWhitespaces</h2><p><em>当<code>preserveWhitespaces</code>设置为<code>false</code>时，多余的空白符（正则匹配<code>\s</code>）会从已编译的模板中移除，很大程度上减小AOT（</em>Ahead-of-Time）预编译模式生成的代码大小且加速视图创建*</p>
<p><em>模板转换处理会潜在的影响页面布局，为了最小化这种影响，从Angular6开始<code>preserveWhitespaces</code>默认设置为<code>false</code>。可以修改AOT编译器的<code>preserveWhitespaces</code>选项使应用内的所有组件都保留空白</em></p>
<p>当前执行遵循以下规则：</p>
<ul>
<li>模板开始和结束的空白都会被删除</li>
<li>仅由空白组成的文本节点会被删除：<code>&lt;button&gt;Action 1&lt;/button&gt;  &lt;button&gt;Action 2&lt;/button&gt;</code>被处理为<code>&lt;button&gt;Action 1&lt;/button&gt;&lt;button&gt;Action 2&lt;/button&gt;</code></li>
<li>文本节点中一系列的空白会被替换为一个空格：<code>&lt;span&gt;\n some text\n&lt;/span&gt;</code>被处理为<code>&lt;span&gt; some text &lt;/span&gt;</code></li>
<li>如果空白是必不可少的，可以用<code>&lt;pre&gt;</code>或<code>&lt;textarea&gt;</code>标签包裹保留空白</li>
</ul>
<p>尽管有默认的<code>preserveWhitespaces:false</code>处理空白，依然可以在DOM元素上添加<code>ngPreserveWhitespaces</code>属性保留空白</p>
<pre><code>&lt;div ngPreserveWhitespaces&gt;
    whitespaces are preserved here
    &lt;span&gt;    and here &lt;/span&gt;
&lt;/div&gt;
</code></pre><p>或者使用<code>&amp;ngsp;</code>作为文本节点，Angular编译器会转换为空格（多个<code>&amp;ngsp;</code>会被编译为一个）：</p>
<pre><code>&lt;a&gt;Spaces&lt;/a&gt;&amp;ngsp;&lt;a&gt;between&lt;/a&gt;&amp;ngsp;&amp;ngsp;&amp;ngsp;&lt;a&gt;links.&lt;/a&gt;
//=&gt; 编译为 &lt;a&gt;Spaces&lt;/a&gt; &lt;a&gt;between&lt;/a&gt; &lt;a&gt;links.&lt;/a&gt;
</code></pre><h2 id="animations"><a href="#animations" class="headerlink" title="animations"></a>animations</h2><p><em>通过监听元素上的状态改变来执行<a href="#docs/animations">animations</a>动画，动画的状态变化存放在<code>trigger</code>中，<code>state</code>声明状态，<code>transition</code>控制状态变化</em></p>
<pre><code>@Component({
    selector: &apos;animation-cmp&apos;,
    templateUrl: &apos;animation-cmp.html&apos;,
    animations: [
      trigger(&apos;myTriggerName&apos;, [
        state(&apos;on&apos;, style({ opacity: 1 }),
        state(&apos;off&apos;, style({ opacity: 0 }),
        transition(&apos;on =&gt; off&apos;, [
          animate(&quot;1s&quot;)
        ])
      ])
    ]
 })
 &lt;div @myTriggerName=&quot;expression&quot;&gt;...&lt;/div&gt;
 //=&gt; expression为当前动画执行到的状态值
</code></pre>]]></content>
      
        
        <tags>
            
            <tag> Angular6.0 </tag>
            
        </tags>
        
    </entry>
    
    <entry>
      <title><![CDATA[指令]]></title>
      <url>/2018/05/11/directive/</url>
      <content type="html"><![CDATA[<h2 id="description"><a href="#description" class="headerlink" title="description"></a>description</h2><p>指令可以在DOM元素上附加行为，一个指令必须属于<code>NgModule</code>模块，在<code>NgModule</code>的<code>declarations</code>字段中声明，以便其他的指令、组件或应用使用。</p>
<h2 id="how-to-use"><a href="#how-to-use" class="headerlink" title="how to use"></a>how to use</h2><pre><code>import { Directive } from &apos;@angular/core&apos;;
@Directive({
    selector?: string,                      //=&gt; 定义组件在HTML代码中匹配的标签
    inputs  ?: string[],                    //=&gt; 指令的输入属性集合
    outputs ?: string[],                    //=&gt; 指令的输出属性集合
    host    ?: {                            //=&gt; 绑定与宿主元素相关的事件、属性
        [key: string]: string
    },
    providers?: Provider[],                 //=&gt; 注入该指令/组件或子组件可用的服务集合
    exportAs ?: string,                     //=&gt; 定义可在模板中使用的名称，以将此指令分配给变量
    queries  ?: {                           //=&gt; 配置指令的查询条件
        [key: string]: any
    }
})
export class DirectiveName{
}
</code></pre><h2 id="selector"><a href="#selector" class="headerlink" title="selector"></a>selector</h2><ul>
<li><code>element-name</code>: 元素名称</li>
<li><code>.class</code>: 元素类名</li>
<li><code>[attribute]</code>: 元素属性名</li>
<li><code>[attribute=value]</code>: 元素属性名/值</li>
<li><code>:not(sub_selector)</code>: 非匹配元素</li>
<li><code>selector1, selector2</code>: 匹配 <code>selector1</code> 或 <code>selector2</code> </li>
</ul>
<h2 id="inputs"><a href="#inputs" class="headerlink" title="inputs"></a>inputs</h2><p><code>inputs</code> 属性定义了<code>[ directiveProperty : bindingProperty]</code> 配置数组:</p>
<ul>
<li><code>directiveProperty</code> 指定渲染指令/组件时的属性名称，在当前指令/组件中使用</li>
<li><code>bindingProperty</code> 指定调用指令/组件时传入的属性名称，在其他指令/组件调用时使用</li>
</ul>
<p>如果<code>bindingProperty</code> 没有定义，和<code>directiveProperty</code>定义的名称相同. </p>
<pre><code>//=&gt; bankAccount.component.ts
import { Component,Input } from &quot;@angular/core&quot;
@Component({
    selector: &apos;bank-account&apos;,
    //inputs: [&apos;bankName&apos;, &apos;id: account-id&apos;],
    template: `
      Bank Name: {{bankName}}
      Account Id: {{id}}
    `
})
export class BankAccountComponent {
    @Input() bankName: string;
    @Input(&apos;account-id&apos;) id: string;
    // this property is not bound, and won&apos;t be automatically updated by Angular
    normalizedBankName: string;
}

//=&gt; app.component.ts
import { Component } from &quot;@angular/core&quot;
@Component({
    selector: &apos;app&apos;,
    template: `
      &lt;bank-account [bankName]=&quot;bank&quot; account-id=&quot;4747&quot;&gt;&lt;/bank-account&gt;
    `
})
export class AppComponent {
    bank : string = &quot;RBC&quot;
}
</code></pre><h2 id="outputs"><a href="#outputs" class="headerlink" title="outputs"></a>outputs</h2><p><code>outputs</code> 属性定义了<code>[ directiveProperty : bindingProperty]</code> 配置数组:</p>
<ul>
<li><code>directiveProperty</code> 指定指令/组件内部触发事件的名称</li>
<li><code>bindingProperty</code> 指定调用指令/组件时附加到DOM元素上的事件名称</li>
</ul>
<pre><code>//=&gt; interval.directive.ts
import { Directive,Output,EventEmitter } from &quot;@angular/core&quot;
@Directive({
    selector: &apos;interval-dir&apos;,
    //outputs: [&apos;everySecond&apos;, &apos;five5Secs: everyFiveSeconds&apos;]
})
export class IntervalDirDirective {
    @Output() everySecond = new EventEmitter();
    @Output(&apos;everyFiveSeconds&apos;) five5Secs = new EventEmitter();
    constructor() {
      setInterval(() =&gt; this.everySecond.emit(&quot;event&quot;), 1000);
      setInterval(() =&gt; this.five5Secs.emit(&quot;event&quot;), 5000);
    }
}

//=&gt; app.component.ts
@Component({
    selector: &apos;app&apos;,
    template: `
      &lt;interval-dir (everySecond)=&quot;everySecond()&quot; (everyFiveSeconds)=&quot;everyFiveSeconds()&quot;&gt;
      &lt;/interval-dir&gt;
    `
})
export class AppComponent {
    everySecond() { console.log(&apos;second&apos;); }
    everyFiveSeconds() { console.log(&apos;five seconds&apos;); }
}
</code></pre><h2 id="host"><a href="#host" class="headerlink" title="host"></a>host</h2><ul>
<li>HostListener宿主元素绑定事件 </li>
</ul>
<pre><code>//=&gt; countClicks.directive.ts
import {Directive,HostListener} from &quot;@angular/core&quot;
@Directive({
    selector: &apos;button[counting]&apos;,
    //host: {
    //    &apos;(click)&apos;: &apos;onClick($event.target)&apos;
        //}
})
export class CountClicks {
    numberOfClicks = 0;

    @HostListener(&apos;click&apos;, [&apos;$event.target&apos;])
    onClick(btn) {
      console.log(&apos;button&apos;, btn, &apos;number of clicks:&apos;, this.numberOfClicks++);
    }
}

//=&gt; app.component.ts
@Component({
    selector: &apos;app&apos;,
    template: &apos;&lt;button counting&gt;Increment&lt;/button&gt;&apos;,
})
export class AppComponent {}
</code></pre><ul>
<li>HostBinding宿主元素绑定属性</li>
</ul>
<pre><code>//=&gt; typeStatus.directive.ts
import { Directive,HostBinding } from &quot;@angular/core&quot;
@Directive({
    selector: &apos;[ngModel]&apos;,
    //host: {
    //  &apos;type&apos;: &apos;password&apos;
    //}
})
export class TypeStatus {
constructor() {}
    @HostBinding(&apos;attr.type&apos;) type = &quot;password&quot;
}

//=&gt; app.component.ts
@Component({
    selector: &apos;app&apos;,
    template: `&lt;input [ngModel]&gt;`,
})
export class AppComponent {}
//=&gt; &lt;input class=&quot;ng-untouched ng-pristine ng-valid&quot; type=&quot;password&quot;&gt;
</code></pre><h2 id="providers"><a href="#providers" class="headerlink" title="providers"></a>providers</h2><pre><code>//=&gt; logger.service.ts
import { Injectable } from &quot;@angular/core&quot;
@Injectable()
export class LoggerService {
    constructor(){}
    time(){
        return new Date()
    }
}

//=&gt; time.directive.ts
@Directive({
  selector: &apos;time&apos;,
  providers: [
    LoggerService
  ]
})
export class TimeDirective {
    tiem:any;

    constructor(log:LoggerService){
        this.time = log.time()
        console.log(this.time)
    }
}
</code></pre><h2 id="exportAs"><a href="#exportAs" class="headerlink" title="exportAs"></a>exportAs</h2><pre><code>//=&gt; time.directive.ts
@Directive({
  selector: &apos;time&apos;,
  providers: [
    LoggerService
  ],
  exportAs:&quot;T&quot;
})
export class TimeDirective{
    time:any;

    constructor(log:LoggerService){
        this.time = log.time()
    }
}

//=&gt; app.component.ts
@Component({
  selector: &apos;app&apos;,
  template: `&lt;time #c=&quot;T&quot;&gt;{{c.time}}&lt;/time&gt;`
})
class AppComponent {
}
</code></pre><h2 id="queries"><a href="#queries" class="headerlink" title="queries"></a>queries</h2><p>内容查询在<code>ngAfterContentInit</code>的回调函数之前执行</p>
<p>视图查询在<code>ngAfterViewInit</code>的回调函数之前执行</p>
<pre><code>@Component({
  selector: &apos;someDir&apos;,
  queries: {
    contentChildren: new ContentChildren(ChildDirective),
    viewChildren: new ViewChildren(ChildDirective)
  },
  template: &apos;&lt;child-directive&gt;&lt;/child-directive&gt;&apos;
})
export class SomeDir {
  contentChildren: QueryList&lt;ChildDirective&gt;,
  viewChildren: QueryList&lt;ChildDirective&gt;
  ngAfterContentInit() {
    // contentChildren is set
  }
  ngAfterViewInit() {
    // viewChildren is set
  }
}
</code></pre>]]></content>
      
        
        <tags>
            
            <tag> Angular6.0 </tag>
            
        </tags>
        
    </entry>
    
    <entry>
      <title><![CDATA[生命周期]]></title>
      <url>/2018/04/20/lifecycle/</url>
      <content type="html"><![CDATA[<h2 id="钩子函数和执行顺序"><a href="#钩子函数和执行顺序" class="headerlink" title="钩子函数和执行顺序"></a>钩子函数和执行顺序</h2><p><img src="https://github.com/Leooman/NgAdmin/raw/gh-pages/images/lifecycle.png" alt="lifecycle"></p>
<h2 id="ngOnChanges"><a href="#ngOnChanges" class="headerlink" title="ngOnChanges"></a>ngOnChanges</h2><p><code>ngOnChanges(changes: SimpleChanges): void;</code></p>
<p>当指令/组件绑定的输入属性的值发生变化时调用</p>
<p>该方法接收一个包含当前和先前属性值的<code>SimpleChanges</code>对象</p>
<p>首次调用发生在ngOnInit()之前，Angular只会在输入属性的值变化时调用这个钩子，非输入属性的值变化不会触发</p>
<pre><code>export declare class SimpleChange {
    previousValue: any;
    currentValue: any;
    firstChange: boolean;
    constructor(previousValue: any, currentValue: any, firstChange: boolean);
    /**
     * 检测是不是第一次赋值
     */
    isFirstChange(): boolean;
}
export interface SimpleChanges { [propName: string]: SimpleChange; }
</code></pre><h2 id="ngOnInit"><a href="#ngOnInit" class="headerlink" title="ngOnInit"></a>ngOnInit</h2><p><code>ngOnInit(): void;</code></p>
<p>在指令/组件的输入属性初始化（第一次<code>ngOnChanges()</code>）之后调用,只在指令/组件实例化时调用一次</p>
<p><code>constructor</code>构造函数中一般用于依赖注入或简单的数据初始化操作，而<code>ngOnInit()</code>可以进行复杂的初始化逻辑获取数据</p>
<h2 id="ngDoCheck"><a href="#ngDoCheck" class="headerlink" title="ngDoCheck"></a>ngDoCheck</h2><p><code>ngDoCheck(): void;</code></p>
<p>当指令/组件进行变化检测时调用，在<code>ngOnChanges()</code>和<code>ngOnInit()</code>之后调用</p>
<p><em>一次变化检测周期中，每一处变化都会触发这个函数</em></p>
<h2 id="ngOnDestroy"><a href="#ngOnDestroy" class="headerlink" title="ngOnDestroy"></a>ngOnDestroy</h2><p><code>ngOnDestroy(): void;</code></p>
<p>在Angular销毁指令/组件之前调用，可进行取消订阅可观察对象和分离事件处理器，以防内存泄漏。</p>
<h2 id="ngAfterContentInit"><a href="#ngAfterContentInit" class="headerlink" title="ngAfterContentInit"></a>ngAfterContentInit</h2><p><code>ngAfterContentInit(): void;</code></p>
<p>当组件的内容初始化完成后调用。第一次<code>ngDoCheck()</code>之后调用，只调用一次。<code>只适用于组件。</code></p>
<h2 id="ngAfterContentChecked"><a href="#ngAfterContentChecked" class="headerlink" title="ngAfterContentChecked"></a>ngAfterContentChecked</h2><p><code>ngAfterContentChecked(): void;</code></p>
<p>每次完成组件内容的变更检测之后调用。<code>ngAfterContentInit()</code>和每次<code>ngDoCheck()</code>之后调用。<code>只适合组件。</code></p>
<h2 id="ngAfterViewInit"><a href="#ngAfterViewInit" class="headerlink" title="ngAfterViewInit"></a>ngAfterViewInit</h2><p><code>ngAfterViewInit(): void;</code></p>
<p>初始化完组件视图及其子视图之后调用。第一次<code>ngAfterContentChecked()</code>之后调用，只调用一次。<code>只适合组件。</code></p>
<h2 id="ngAfterViewChecked"><a href="#ngAfterViewChecked" class="headerlink" title="ngAfterViewChecked"></a>ngAfterViewChecked</h2><p><code>ngAfterViewChecked(): void;</code></p>
<p>每次组件视图和子视图的变更检测之后调用。<code>ngAfterViewInit()</code>和每次<code>ngAfterContentChecked()</code>之后调用。<code>只适合组件。</code></p>
]]></content>
      
        
        <tags>
            
            <tag> Angular6.0 </tag>
            
        </tags>
        
    </entry>
    
    <entry>
      <title><![CDATA[公众号支付]]></title>
      <url>/2018/01/27/wxPayService/</url>
      <content type="html"><![CDATA[<blockquote>
<p><strong>微信公众号支付类</strong></p>
</blockquote>
<pre><code>class WxPayService{
    //=======【证书路径设置】=====================================
    //证书路径,注意应该填写绝对路径
    protected $SSLCERT_PATH = &apos;apiclient_cert.pem&apos;;
    protected $SSLKEY_PATH =  &apos;apiclient_key.pem&apos;;
    //=======【基本信息设置】=====================================
    protected $APPID = &apos;&apos;;//填写微信公众号appid
    protected $MCHID = &apos;&apos;;//支付商户号
    protected $KEY = &apos;&apos;; //商户支付API密钥
    protected $APPURL = &apos;https://api.mch.weixin.qq.com/pay/unifiedorder&apos;;//API请求地址

    /**
     * 微信公众号支付主函数
     */
    function pay($body,$fee,$openid, $notify_url){
        $Parameters=array();
          $Parameters[&apos;appid&apos;]=$this-&gt;APPID;
        $Parameters[&apos;mch_id&apos;]=$this-&gt;MCHID;
        $Parameters[&apos;spbill_create_ip&apos;]=$_SERVER[&apos;SERVER_ADDR&apos;];
        $Parameters[&apos;nonce_str&apos;]=$this-&gt;createNonceStr();
        $Parameters[&apos;body&apos;]=$body;//商品描述
        $Parameters[&apos;out_trade_no&apos;]=&apos;Leooman&apos;.time().rand(10000, 99999);//自定义订单号，保持唯一性
        $Parameters[&apos;total_fee&apos;]=$fee;
        $Parameters[&apos;notify_url&apos;]=$notify_url;//通知地址
        $Parameters[&apos;trade_type&apos;]=&quot;JSAPI&quot;;//交易类型
        $Parameters[&apos;openid&apos;]=$openid;//交易类型为JSAPI时此参数必填
        $Parameters[&apos;sign&apos;]=$this-&gt;getSign($Parameters);
        //选填参数
        //$Parameters[&apos;attach&apos;]=&quot;微信支付&quot;;//附加数据
        //$Parameters[&apos;sub_mch_id&apos;]=&quot;&quot;;//子商户号
        //$Parameters[&apos;device_info&apos;]=&quot;&quot;;//设备号
        //$Parameters[&apos;goods_tag&apos;]=&quot;&quot;;//商品标记
        //$Parameters[&apos;product_id&apos;]=&quot;&quot;;//商品ID
        //$Parameters[&apos;time_start&apos;]=date(&quot;YmdHis&quot;);//交易起始时间
        //$Parameters[&apos;time_expire&apos;]=date(&quot;YmdHis&quot;, time() + 600);//交易结束时间
        $xml=$this-&gt;arrayToXml($Parameters);
        $response=$this-&gt;postXmlSSLCurl($xml,$this-&gt;APPURL);
        $result=$this-&gt;xmlToArray($response);
        $jsApiParameters = $this-&gt;GetJsApiParameters($result);
        return $jsApiParameters;
    }

    function GetJsApiParameters($UnifiedOrderResult){
        $orderParameters[&apos;appId&apos;]=$UnifiedOrderResult[&quot;appid&quot;];
        $orderParameters[&apos;timeStamp&apos;]=time();
        $orderParameters[&apos;nonceStr&apos;]=$UnifiedOrderResult[&quot;nonce_str&quot;];
        $orderParameters[&apos;package&apos;]=&quot;prepay_id=&quot; . $UnifiedOrderResult[&apos;prepay_id&apos;];
        $orderParameters[&apos;signType&apos;]=&quot;MD5&quot;;
        $orderParameters[&apos;paySign&apos;]=$this-&gt;getSign($orderParameters);
        $parameters = json_encode($orderParameters);
        return $parameters;
    }

    /**
     *  作用：格式化参数，签名过程需要使用
     */
    function formatBizQueryParaMap($paraMap, $urlencode)
    {
        $buff = &quot;&quot;;
        ksort($paraMap);
        foreach ($paraMap as $k =&gt; $v)
        {
            if($urlencode)
            {
               $v = urlencode($v);
            }
            $buff .= $k . &quot;=&quot; . $v . &quot;&amp;&quot;;
        }
        $reqPar;
        if (strlen($buff) &gt; 0) 
        {
            $reqPar = substr($buff, 0, strlen($buff)-1);
        }
        return $reqPar;
    }

    /**
     *  作用：签名生成
     */
    function getSign($Obj)
    {
        foreach ($Obj as $k =&gt; $v)
        {
            $Parameters[$k] = $v;
        }
        //签名步骤一：按字典序排序参数
        ksort($Parameters);
        $String = $this-&gt;formatBizQueryParaMap($Parameters, false);
        //签名步骤二：在string后加入KEY
        $String = $String.&quot;&amp;key=&quot;.$this-&gt;KEY;
        //签名步骤三：MD5加密
        $String = md5($String);
        //签名步骤四：所有字符转为大写
        $result_ = strtoupper($String);
        return $result_;
    }

    /**
     *  作用：产生随机字符串，不长于32位
     */
    function createNoncestr( $length = 32 ) 
    {
        $chars = &quot;abcdefghijklmnopqrstuvwxyz0123456789&quot;;  
        $str =&quot;&quot;;
        for ( $i = 0; $i &lt; $length; $i++ )  {  
            $str.= substr($chars, mt_rand(0, strlen($chars)-1), 1);  
        }  
        return $str;
    }

    /**
     *  作用：array转xml
     */
    function arrayToXml($arr)
    {
        $xml = &quot;&lt;xml&gt;&quot;;
        foreach ($arr as $key=&gt;$val)
        {
            if (is_numeric($val)){
                $xml.=&quot;&lt;&quot;.$key.&quot;&gt;&quot;.$val.&quot;&lt;/&quot;.$key.&quot;&gt;&quot;;
            }else{
                $xml.=&quot;&lt;&quot;.$key.&quot;&gt;&lt;![CDATA[&quot;.$val.&quot;]]&gt;&lt;/&quot;.$key.&quot;&gt;&quot;;
            }
        }
        $xml.=&quot;&lt;/xml&gt;&quot;;
        return $xml; 
    }

    /**
     *  作用：将xml转为array
     */
    function xmlToArray($xml)
    {        
        $array_data = json_decode(json_encode(simplexml_load_string($xml, &apos;SimpleXMLElement&apos;, LIBXML_NOCDATA)), true);      
        return $array_data;
    }

    /**
     *  作用：使用证书，以post方式提交xml到对应的接口url
     */
    function postXmlSSLCurl($xml,$url,$second=30)
    {
        $ch = curl_init();
        //超时时间
        curl_setopt($ch,CURLOPT_TIMEOUT,$second);
        //这里设置代理，如果有的话
        //curl_setopt($ch,CURLOPT_PROXY, &apos;8.8.8.8&apos;);
        //curl_setopt($ch,CURLOPT_PROXYPORT, 8080);
        curl_setopt($ch,CURLOPT_URL, $url);
        curl_setopt($ch,CURLOPT_SSL_VERIFYPEER,FALSE);
        curl_setopt($ch,CURLOPT_SSL_VERIFYHOST,FALSE);
        //设置header
        curl_setopt($ch,CURLOPT_HEADER,FALSE);
        //要求结果为字符串且输出到屏幕上
        curl_setopt($ch,CURLOPT_RETURNTRANSFER,TRUE);
        //设置证书
        //使用证书：cert 与 key 分别属于两个.pem文件
        //默认格式为PEM，可以注释
        curl_setopt($ch,CURLOPT_SSLCERTTYPE,&apos;PEM&apos;);
        // 自行修改成绝对路径
        curl_setopt($ch,CURLOPT_SSLCERT, dirname(__FILE__).DIRECTORY_SEPARATOR.&apos;cert&apos;.DIRECTORY_SEPARATOR.$this-&gt;SSLCERT_PATH);
        //默认格式为PEM，可以注释
        curl_setopt($ch,CURLOPT_SSLKEYTYPE,&apos;PEM&apos;);
        // 自行修改成绝对路径
        curl_setopt($ch,CURLOPT_SSLKEY, dirname(__FILE__).DIRECTORY_SEPARATOR.&apos;cert&apos;.DIRECTORY_SEPARATOR.$this-&gt;SSLKEY_PATH);
        //post提交方式
        curl_setopt($ch,CURLOPT_POST, true);
        curl_setopt($ch,CURLOPT_POSTFIELDS,$xml);
        $data = curl_exec($ch);
        //返回结果
        if($data){
            curl_close($ch);
            return $data;
        }
        else { 
            $error = curl_errno($ch);
            echo &quot;curl出错，错误码:$error&quot;.&quot;&quot;; 
            curl_close($ch);
            return false;
        }
    }
}
</code></pre><blockquote>
<p><strong>调用类</strong></p>
</blockquote>
<pre><code>$wxPay = new WxPayService();
$wxPay-&gt;pay(&apos;商品描述&apos;, 金额, &apos;用户openid&apos;, &apos;通知地址&apos;);
</code></pre><blockquote>
<p><strong>以post方式提交到接口url的xml必填数据</strong></p>
</blockquote>
<pre><code>&lt;xml&gt;
  &lt;openid&gt;&lt;![CDATA[用户openid]]&gt;&lt;/openid&gt;
  &lt;body&gt;&lt;![CDATA[商品描述]]&gt;&lt;/body&gt;
  &lt;out_trade_no&gt;&lt;![CDATA[Leooman121454512122]]&gt;&lt;/out_trade_no&gt;
  &lt;total_fee&gt;1&lt;/total_fee&gt;
  &lt;notify_url&gt;&lt;![CDATA[通知地址]]&gt;&lt;/notify_url&gt;
  &lt;trade_type&gt;&lt;![CDATA[JSAPI]]&gt;&lt;/trade_type&gt;
  &lt;appid&gt;&lt;![CDATA[商户appid]]&gt;&lt;/appid&gt;
  &lt;mch_id&gt;支付商户号&lt;/mch_id&gt;
  &lt;spbill_create_ip&gt;&lt;![CDATA[客户端ip地址]]&gt;&lt;/spbill_create_ip&gt;
  &lt;nonce_str&gt;&lt;![CDATA[60uf9sh6nmppr9azveb2bn7arhy79izk]]&gt;&lt;/nonce_str&gt;
  &lt;sign&gt;&lt;![CDATA[签名]]&gt;&lt;/sign&gt;
&lt;/xml&gt;
</code></pre><blockquote>
<p><strong>从接口url返回的xml数据</strong></p>
</blockquote>
<pre><code>&lt;xml&gt;
  &lt;return_code&gt;&lt;![CDATA[SUCCESS]]&gt;&lt;/return_code&gt;  
  &lt;return_msg&gt;&lt;![CDATA[OK]]&gt;&lt;/return_msg&gt;  
  &lt;appid&gt;&lt;![CDATA[商户appid]]&gt;&lt;/appid&gt;  
  &lt;mch_id&gt;&lt;![CDATA[支付商户号]]&gt;&lt;/mch_id&gt;  
  &lt;nonce_str&gt;&lt;![CDATA[Be8YX7gjCdtCT7cr]]&gt;&lt;/nonce_str&gt;  
  &lt;sign&gt;&lt;![CDATA[签名]]&gt;&lt;/sign&gt;  
  &lt;prepay_id&gt;&lt;![CDATA[预支付ID参数]]&gt;&lt;/prepay_id&gt;  
  &lt;trade_type&gt;&lt;![CDATA[JSAPI]]&gt;&lt;/trade_type&gt; 
&lt;/xml&gt;
</code></pre><blockquote>
<p><strong>主函数返回json数据</strong></p>
</blockquote>
<pre><code>{
    &quot;appId&quot;: &quot;商户appid&quot;,
    &quot;timeStamp&quot;: &quot;时间戳&quot;,
    &quot;nonceStr&quot;: &quot;随机字符串&quot;,
    &quot;package&quot;: &quot;prepay_id=预支付ID参数&quot;,
    &quot;signType&quot;: &quot;MD5&quot;,
    &quot;paySign&quot;: &quot;支付签名&quot;
}
</code></pre><blockquote>
<p><strong>前端调用</strong></p>
</blockquote>
<pre><code>&lt;html&gt;
&lt;head&gt;
    &lt;meta http-equiv=&quot;content-type&quot; content=&quot;text/html;charset=utf-8&quot;/&gt;
    &lt;meta name=&quot;viewport&quot; content=&quot;width=device-width, initial-scale=1&quot;/&gt; 
    &lt;title&gt;微信支付&lt;/title&gt;
    &lt;script type=&quot;text/javascript&quot;&gt;
    function jsApiCall()
    {
        WeixinJSBridge.invoke(
            &apos;getBrandWCPayRequest&apos;,
            {jsApiParameters},//主函数pay返回的json数据
            function(res){
                WeixinJSBridge.log(res.err_msg);
                alert(res.err_code+res.err_desc+res.err_msg);
            }
        );
    }
    function callpay()
    {
        if (typeof WeixinJSBridge == &quot;undefined&quot;){
            if( document.addEventListener ){
                document.addEventListener(&apos;WeixinJSBridgeReady&apos;, jsApiCall, false);
            }else if (document.attachEvent){
                document.attachEvent(&apos;WeixinJSBridgeReady&apos;, jsApiCall); 
                document.attachEvent(&apos;onWeixinJSBridgeReady&apos;, jsApiCall);
            }
        }else{
            jsApiCall();
        }
    }
    &lt;/script&gt;
&lt;/head&gt;
&lt;body&gt;
    &lt;div align=&quot;center&quot;&gt;
        &lt;button onclick=&quot;callpay()&quot; &gt;立即支付&lt;/button&gt;
    &lt;/div&gt;
&lt;/body&gt;
&lt;/html&gt;
</code></pre><blockquote>
<p><strong>参考文档</strong></p>
</blockquote>
<p><a href="https://pay.weixin.qq.com/wiki/doc/api/jsapi.php?chapter=4_3" target="_blank" rel="noopener">微信开发——签名算法</a></p>
<p><a href="https://pay.weixin.qq.com/wiki/doc/api/jsapi.php?chapter=7_7&amp;index=6" target="_blank" rel="noopener">微信开发——微信内H5调起支付</a></p>
]]></content>
      
        
        <tags>
            
            <tag> 微信支付 </tag>
            
        </tags>
        
    </entry>
    
    <entry>
      <title><![CDATA[微信企业付款]]></title>
      <url>/2018/01/08/wxPay/</url>
      <content type="html"><![CDATA[<blockquote>
<p><strong>企业付款至零钱</strong></p>
</blockquote>
<pre><code>class wxPay{
    //=======【证书路径设置】=====================================
    //证书路径,注意应该填写绝对路径
    protected $SSLCERT_PATH = &apos;apiclient_cert.pem&apos;;
    protected $SSLKEY_PATH =  &apos;apiclient_key.pem&apos;;
    //=======【基本信息设置】=====================================
    protected $APPID = &apos;&apos;;//填写微信公众号appid
    protected $MCHID = &apos;&apos;;//支付商户号
    protected $KEY = &apos;&apos;; //商户支付API密钥
    protected $APPURL = &apos;https://api.mch.weixin.qq.com/mmpaymkttransfers/promotion/transfers&apos;;//JSAPI接口地址

    /**
     * 微信企业付款
     * @param  [type] $openid   商户appid下，某用户的openid
     * @param  [type] $username 收款用户真实姓名。
     * @param  [type] $desc     企业付款操作说明信息。必填。
     * @param  [type] $money    企业付款金额，单位为分
     */
    function pay($openid=&apos;&apos;, $username=&apos;&apos;, $desc=&apos;&apos;, $money=0){
        $apiUrl = $this-&gt;APPURL;
        $Parameters=array();
        $Parameters[&apos;amount&apos;]           = $money;//企业付款金额，单位为分
        $Parameters[&apos;check_name&apos;]       = &apos;NO_CHECK&apos;;//NO_CHECK：不校验真实姓名 FORCE_CHECK：强校验真实姓名（未实名认证的用户会校验失败，无法转账） OPTION_CHECK：针对已实名认证的用户才校验真实姓名（未实名认证用户不校验，可以转账成功）
        $Parameters[&apos;desc&apos;]             = $desc;//企业付款操作说明信息。必填。
        $Parameters[&apos;mch_appid&apos;]        = $this-&gt;APPID;//微信分配的公众账号ID
        $Parameters[&apos;mchid&apos;]            = $this-&gt;MCHID;//微信支付分配的商户号
        $Parameters[&apos;nonce_str&apos;]        = $this-&gt;createNoncestr();//随机字符串，不长于32位
        $Parameters[&apos;openid&apos;]           = $openid;//商户appid下，某用户的openid
        $Parameters[&apos;partner_trade_no&apos;] = &apos;Leooman&apos;.time().rand(10000, 99999);//商户订单号，需保持唯一性
        $Parameters[&apos;re_user_name&apos;]     = $username;//收款用户真实姓名。 如果check_name设置为FORCE_CHECK或OPTION_CHECK，则必填用户真实姓名
        $Parameters[&apos;spbill_create_ip&apos;] = $_SERVER[&apos;SERVER_ADDR&apos;];//调用接口的机器Ip地址
        //$Parameters[&apos;spbill_create_ip&apos;] = gethostbyname($_ENV[&apos;COMPUTERNAME&apos;]);//调用接口的机器Ip地址(本地测试调用)
        $Parameters[&apos;sign&apos;]             = $this-&gt;getSign($Parameters);//签名
        $xml  = $this-&gt;arrayToXml($Parameters);
        $res  = $this-&gt;postXmlSSLCurl($xml,$apiUrl);
        $return = $this-&gt;xmlToArray($res);
        return $res;
    }

    /**
     *  作用：格式化参数，签名过程需要使用
     */
    function formatBizQueryParaMap($paraMap, $urlencode)
    {
        $buff = &quot;&quot;;
        ksort($paraMap);
        foreach ($paraMap as $k =&gt; $v)
        {
            if($urlencode)
            {
               $v = urlencode($v);
            }
            $buff .= $k . &quot;=&quot; . $v . &quot;&amp;&quot;;
        }
        $reqPar;
        if (strlen($buff) &gt; 0) 
        {
            $reqPar = substr($buff, 0, strlen($buff)-1);
        }
        return $reqPar;
    }

    /**
     *  作用：签名生成
     */
    function getSign($Obj)
    {
        foreach ($Obj as $k =&gt; $v)
        {
            $Parameters[$k] = $v;
        }
        //签名步骤一：按字典序排序参数
        ksort($Parameters);
        $String = $this-&gt;formatBizQueryParaMap($Parameters, false);
        //签名步骤二：在string后加入KEY
        $String = $String.&quot;&amp;key=&quot;.$this-&gt;KEY;
        //签名步骤三：MD5加密
        $String = md5($String);
        //签名步骤四：所有字符转为大写
        $result_ = strtoupper($String);
        return $result_;
    }

    /**
     *  作用：产生随机字符串，不长于32位
     */
    function createNoncestr( $length = 32 ) 
    {
        $chars = &quot;abcdefghijklmnopqrstuvwxyz0123456789&quot;;  
        $str =&quot;&quot;;
        for ( $i = 0; $i &lt; $length; $i++ )  {  
            $str.= substr($chars, mt_rand(0, strlen($chars)-1), 1);  
        }  
        return $str;
    }

    /**
     *  作用：array转xml
     */
    function arrayToXml($arr)
    {
        $xml = &quot;&lt;xml&gt;&quot;;
        foreach ($arr as $key=&gt;$val)
        {
            if (is_numeric($val)){
                $xml.=&quot;&lt;&quot;.$key.&quot;&gt;&quot;.$val.&quot;&lt;/&quot;.$key.&quot;&gt;&quot;;
            }else{
                $xml.=&quot;&lt;&quot;.$key.&quot;&gt;&lt;![CDATA[&quot;.$val.&quot;]]&gt;&lt;/&quot;.$key.&quot;&gt;&quot;;
            }
        }
        $xml.=&quot;&lt;/xml&gt;&quot;;
        return $xml; 
    }

    /**
     *  作用：将xml转为array
     */
    function xmlToArray($xml)
    {        
        $array_data = json_decode(json_encode(simplexml_load_string($xml, &apos;SimpleXMLElement&apos;, LIBXML_NOCDATA)), true);      
        return $array_data;
    }

    /**
     *  作用：使用证书，以post方式提交xml到对应的接口url
     */
    function postXmlSSLCurl($xml,$url,$second=30)
    {
        $ch = curl_init();
        //超时时间
        curl_setopt($ch,CURLOPT_TIMEOUT,$second);
        //这里设置代理，如果有的话
        //curl_setopt($ch,CURLOPT_PROXY, &apos;8.8.8.8&apos;);
        //curl_setopt($ch,CURLOPT_PROXYPORT, 8080);
        curl_setopt($ch,CURLOPT_URL, $url);
        curl_setopt($ch,CURLOPT_SSL_VERIFYPEER,FALSE);
        curl_setopt($ch,CURLOPT_SSL_VERIFYHOST,FALSE);
        //设置header
        curl_setopt($ch,CURLOPT_HEADER,FALSE);
        //要求结果为字符串且输出到屏幕上
        curl_setopt($ch,CURLOPT_RETURNTRANSFER,TRUE);
        //设置证书
        //使用证书：cert 与 key 分别属于两个.pem文件
        //默认格式为PEM，可以注释
        curl_setopt($ch,CURLOPT_SSLCERTTYPE,&apos;PEM&apos;);
        // 自行修改成绝对路径
        curl_setopt($ch,CURLOPT_SSLCERT, dirname(__FILE__).DIRECTORY_SEPARATOR.&apos;cert&apos;.DIRECTORY_SEPARATOR.$this-&gt;SSLCERT_PATH);
        //默认格式为PEM，可以注释
        curl_setopt($ch,CURLOPT_SSLKEYTYPE,&apos;PEM&apos;);
        // 自行修改成绝对路径
        curl_setopt($ch,CURLOPT_SSLKEY, dirname(__FILE__).DIRECTORY_SEPARATOR.&apos;cert&apos;.DIRECTORY_SEPARATOR.$this-&gt;SSLKEY_PATH);
        //post提交方式
        curl_setopt($ch,CURLOPT_POST, true);
        curl_setopt($ch,CURLOPT_POSTFIELDS,$xml);
        $data = curl_exec($ch);
        //返回结果
        if($data){
            curl_close($ch);
            return $data;
        }
        else { 
            $error = curl_errno($ch);
            echo &quot;curl出错，错误码:$error&quot;.&quot;&quot;; 
            curl_close($ch);
            return false;
        }
    }
}
</code></pre><blockquote>
<p><strong>类调用</strong></p>
</blockquote>
<pre><code>$wxPay = new wxPay();
$wxPay-&gt;pay(&apos;用户openid&apos;, &apos;用户名&apos;, &apos;详细信息&apos;, 金额);
</code></pre><blockquote>
<p><strong>参考文档</strong></p>
</blockquote>
<p><a href="https://pay.weixin.qq.com/wiki/doc/api/jsapi.php?chapter=4_3" target="_blank" rel="noopener">微信开发——签名算法</a></p>
<p><a href="https://pay.weixin.qq.com/wiki/doc/api/tools/mch_pay.php?chapter=14_2" target="_blank" rel="noopener">微信开发——企业付款</a></p>
]]></content>
      
        
        <tags>
            
            <tag> 微信支付 </tag>
            
        </tags>
        
    </entry>
    
    <entry>
      <title><![CDATA[AVL树]]></title>
      <url>/2018/01/07/AVL/</url>
      <content type="html"><![CDATA[<blockquote>
<p><strong>AVL树定义</strong></p>
</blockquote>
<p>AVL树是最先发明的自平衡二叉查找树。为了避免出现二叉排序树的最坏情况，限定在AVL树中每个结点的左右子树的高度之差的绝对值（平衡因子）最多为1，所以它也被称为高度平衡树。</p>
<p>查找、插入和删除在平均和最坏情况下都是O（log n）。</p>
<p>增加和删除可能需要通过一次或多次树旋转来重新平衡这个树。</p>
<p>离插入节点最近的平衡系数的绝对值大于等于二的节点作为根节点所形成的树称为最小非平衡子树（minimum unblanced subtree）</p>
<blockquote>
<p><strong>优缺点</strong></p>
</blockquote>
<p>在建树的过程中保证了树的高度尽可能的低，相对于一般的二叉排序树来说，提高了查找的效率。</p>
<p>在增加和删除结点的时候，要考虑树的平衡情况。当出现某个结点不平衡的情况，就要对首先不平衡的结点进行调整旋转的情况。因此，增加了维护树的开销。</p>
<p>适用于结点多少比较固定，需要较快查询速度的情况。</p>
<blockquote>
<p><strong>树旋转</strong></p>
</blockquote>
<p>假设由于在二叉排序树上插入结点而失去平衡的最小子树根结点的指针为a（即a是离插入点最近，且平衡因子绝对值超过1的祖先结点），则失去平衡后进行进行的规律可归纳为下列四种情况：</p>
<ul>
<li>右单旋平衡处理(非平衡状态均发生在左子树侧：LL)：</li>
</ul>
<p>由于在a的左子树根结点的左子树上插入结点，a的平衡因子由1增至2，致使以a为根的子树失去平衡，则需进行一次右旋转操作；</p>
<p><img src="http://qiniu.leooman.com/AVL/LL.png" alt="右旋转"></p>
<pre><code>function roateRight(AvlNode) {
    var node = AvlNode.left; // 保存左子节点
    AvlNode.left = node.right; // 将node的右子节点连接到AvlNode成为其左子节点
    node.right = AvlNode; // AvlNode连接到node，成为其右子节点
    return node; // 返回node连接到AvlNode最初的父节点
}
</code></pre><ul>
<li>左单旋平衡处理(非平衡状态均发生在右子树侧：RR)：</li>
</ul>
<p>由于在a的右子树根结点的右子树上插入结点，a的平衡因子由-1变为-2，致使以a为根的子树失去平衡，则需进行一次左旋转操作；</p>
<p><img src="http://qiniu.leooman.com/AVL/RR.png" alt="左旋转"></p>
<pre><code>function roateLeft(AvlNode) {
    var node = AvlNode.right; // 保存右子节点
    AvlNode.right = node.left; // node的左子节点连接到AvlNode成为其右子节点
    node.left = AvlNode; // AvlNode连接到node成为其左子节点
    return node; // 返回node，连接到AvlNode最初的父节点
}
</code></pre><ul>
<li>左-右双旋平衡处理(非平衡状态从上到下先发生在左子树，再发生到整棵树：LR)：</li>
</ul>
<p>由于在a的左子树根结点的右子树上插入结点，a的平衡因子由1增至2，致使以a为根的子树失去平衡，则需进行两次旋转（先左旋后右旋）操作。</p>
<p><img src="http://qiniu.leooman.com/AVL/LR.png" alt="左-右旋转"></p>
<pre><code>function roateLeftRight(AvlNode) {
    AvlNode.right = roateLeft(AvlNode.right); // 对右子节点做左单旋
    return roateRight(AvlNode); // 做右单旋
}
</code></pre><ul>
<li>右-左双旋平衡处理(非平衡状态从上到下先发生在右子树，再发生到整棵树：RL)：</li>
</ul>
<p>由于在a的右子树根结点的左子树上插入结点，a的平衡因子由-1变为-2，致使以a为根的子树失去平衡，则需进行两次旋转（先右旋后左旋）操作。</p>
<pre><code>function roateRightLeft(AvlNode) {
    AvlNode.left = roateRight(AvlNode.left); // 对左子节点做右单旋
    return roateLeft(AvlNode); // 做左单旋
}
</code></pre><blockquote>
<p><strong>获取树高度</strong></p>
</blockquote>
<pre><code>function getAvlTreeHeight(node) {
    if (node == null) {
        // node不存在返回0
        return 0;
    } else {
        var leftHeight = getAvlTreeHeight(node.left);
        var rightHeight = getAvlTreeHeight(node.right);
        // 返回左子树、右子树中的最大高度
        return (leftHeight &gt; rightHeight ? leftHeight : rightHeight) + 1;
    }
}
</code></pre><blockquote>
<p><strong>实现平衡</strong></p>
</blockquote>
<pre><code>function balance(node) {
    if (node == null) {
        return node;
    }
    // 左子树高度比右子树高度大1以上
    if (getAvlTreeHeight(node.left) - getAvlTreeHeight(node.right) &gt; 1) {
        if (getAvlTreeHeight(node.left.left) &gt;= getAvlTreeHeight(node.left.right)) {
            // 如果左子树的左子树高度大于等于左子树的右子树高度
            // 直接进行右单旋
            node = roateRight(node);
        } else {
            // 否则需要右-左双旋
            node = roateRightLeft(node);
        }
        // 右子树高度比左子树高度大1以上
    } else if (getAvlTreeHeight(node.right) - getAvlTreeHeight(node.left) &gt; 1) {
        if (getAvlTreeHeight(node.right.right) &gt;= getAvlTreeHeight(node.right.left)) {
            // 如果右子树的右子树高度大于等于右子树的左子树高度
            // 直接进行左单旋
            node = roateLeft(node);
        } else {
            // 否则需要左-右双旋
            node = roateLeftRight(node);
        }
    }
    return node;
}
</code></pre><blockquote>
<p><strong>插入节点</strong></p>
</blockquote>
<pre><code>var insertNode = function(node, newNode){
    if (newNode.key &lt; node.key){
        if (node.left === null){
            node.left = newNode;
            // 插入节点后，做树的平衡处理
            node.left = balance(node.left);
        } else {
            insertNode(node.left, newNode);
        }
    } else {
        if (node.right === null){
            node.right = newNode;
            // 插入节点后，做树的平衡处理
            node.right = balance(node.right);
        } else {
            insertNode(node.right, newNode);
        }
    }
}
</code></pre><blockquote>
<p><strong>完整实现代码</strong></p>
</blockquote>
<p><a href="https://github.com/Leooman/Leooman.github.io/issues/3" target="_blank" rel="noopener">https://github.com/Leooman/Leooman.github.io/issues/3</a></p>
]]></content>
      
        
        <tags>
            
            <tag> javascript </tag>
            
        </tags>
        
    </entry>
    
    <entry>
      <title><![CDATA[排序二叉树]]></title>
      <url>/2018/01/07/binarytree/</url>
      <content type="html"><![CDATA[<blockquote>
<p><strong>二叉排序树</strong></p>
</blockquote>
<p>二叉排序树（Binary Sort Tree），又称二叉查找树（Binary Search Tree），亦称二叉搜索树。</p>
<p>二叉排序树是一种比较有用的折衷方案。数组的搜索比较方便，可以直接用下标。链表与之相反，删除和插入元素很快，但查找很慢。二叉排序树就既有链表的好处，也有数组的好处。在处理大批量的动态的数据是比较有用。</p>
<p>二叉排序树或者是一棵空树，或者是具有下列性质的二叉树：</p>
<ul>
<li><p>若左子树不空，则左子树上所有结点的值均小于或等于它的根结点的值；</p>
</li>
<li><p>若右子树不空，则右子树上所有结点的值均大于或等于它的根结点的值；</p>
</li>
<li><p>左、右子树也分别为二叉排序树；</p>
</li>
</ul>
<blockquote>
<p><strong>创建二叉树</strong></p>
</blockquote>
<pre><code>function BinaryTree(){
    var Node=function(key){
        this.key=key
        this.left=null
        this.right=null
    }
    var root = null
    var insertNode=function(node,newNode){
        if(newNode.key &lt; node.key){
            if(node.left===null){
                node.left=newNode
            }else{
                insertNode(node.left,newNode)
            }
        }else{
            if(node.right===null){
                node.right=newNode
            }else{
                insertNode(node.right,newNode)
            }
        }
    }
    // 创建二叉树
    this.insert=function(key){
        var node=new Node(key)
        if(root === null){
            root=node
        }else{
            insertNode(root,node)
        }
    }
}
</code></pre><blockquote>
<p><strong>先序遍历 复制二叉树</strong></p>
</blockquote>
<p>先序遍历：（1）访问根节点；（2）采用先序递归遍历左子树；（3）采用先序递归遍历右子树</p>
<pre><code>this.preOrderTraverse=function(callback){
    preOrderTraverseNode(root,callback)
}
var preOrderTraverseNode=function(node,callback){
    if(node!==null){
        callback(node.key)
        preOrderTraverseNode(node.left,callback)
        preOrderTraverseNode(node.right,callback)
    }
}
</code></pre><blockquote>
<p><strong>后序遍历</strong></p>
</blockquote>
<p>后序遍历：（1）采用后序递归遍历左子树；（2）采用后序递归遍历右子树；（3）访问根节点</p>
<pre><code>this.postOrderTraverse=function(callback){
    postOrderTraverseNode(root,callback)
}
var postOrderTraverseNode=function(node,callback){
    if(node!==null){
        postOrderTraverseNode(node.left,callback)
        postOrderTraverseNode(node.right,callback)
        callback(node.key)
    }
}
</code></pre><blockquote>
<p><strong>中序遍历 升序排列</strong></p>
</blockquote>
<p>中序遍历：（1）采用中序遍历左子树；（2）访问根节点；（3）采用中序遍历右子树</p>
<pre><code>this.inOrderTraverse=function(callback){
    inOrderTraverseNode(root,callback)
}
var inOrderTraverseNode=function(node,callback){
    if(node!==null){    
        inOrderTraverseNode(node.left,callback)
        callback(node.key)
        inOrderTraverseNode(node.right,callback)
    }
}
</code></pre><blockquote>
<p><strong>查找最小值</strong></p>
</blockquote>
<pre><code>this.min=function(){
    return minNode(root)
}
var minNode=function(node){
    if(node){
        while(node &amp;&amp; node.left!==null){
            node=node.left
        }
        return node.key
    }
}
</code></pre><blockquote>
<p><strong>查找最大值</strong></p>
</blockquote>
<pre><code>this.max=function(){
    return maxNode(root)
}
var maxNode=function(node){
    if(node){
        while(node &amp;&amp; node.right!==null){
            node=node.right
        }
        return node.key
    }
}
</code></pre><blockquote>
<p><strong>查找是否存在对应值</strong></p>
</blockquote>
<pre><code>this.search=function(key){
    return searchNode(root,key)
}
var searchNode=function(node,key){
    if(node === null) return false
    if(key &lt; node.key){
        return searchNode(node.left,key)
    }else if(key &gt; node.key){
        return searchNode(node.right,key)
    }else{
        return true
    }
}
</code></pre><blockquote>
<p><strong>删除节点</strong></p>
</blockquote>
<pre><code>this.remove=function(key){
    root=removeNode(root,key)
}
var findMinNode=function(node){
    if(node){
        while(node &amp;&amp; node.left!==null){
            node=node.left
        }
        return node
    }
}
var removeNode=function(node,key){
    if(node===null) return null
    if(key &lt; node.key){
        node.left=removeNode(node.left,key)
        return node
    }else if(key &gt; node.right){
        node.right=removeNode(node.right,key)
        return node
    }else{
        // 叶子节点
        if(node.left === null &amp;&amp; node.right === null){
            node = null
            return node
        }
        // 只包含右子树
        if(node.left === null){
            node=node.right
            return node
        }
        // 只包含左子树
        if(node.right === null){
            node=node.left
            return node
        }
        // 包含左右子树(查找右子树的最小节点删除并修改当前节点的key值)
        var aux=findMinNode(node.right)
        node.key=aux.key
        node.right=removeNode(node.right,aux.key)
        return node
    }
}
</code></pre><blockquote>
<p><strong>DEMO</strong></p>
</blockquote>
<p><a href="https://github.com/Leooman/Leooman.github.io/issues/1" target="_blank" rel="noopener">代码测试地址：https://github.com/Leooman/Leooman.github.io/issues/1</a></p>
<blockquote>
<p><strong>性能分析</strong></p>
</blockquote>
<p>每个结点的C(i)查找次数为该结点的层次数。</p>
<p>最坏情况下，当先后插入的关键字有序时，构成的二叉排序树蜕变为单支树，树的深度为其平均查找长度(n+1)/2(和顺序查找相同）</p>
<p><img src="http://qiniu.leooman.com/zlj4n4iefa.jpg" alt="最坏排序树"></p>
<p>最好的情况是二叉排序树的形态和折半查找的判定树相同，其平均查找长度和log 2 (n)成正比。</p>
<p>为了提高二叉排序树的查找效率，需要把树构建得更为平衡，从而不出现左右偏重的情况，这就引出了AVL树(通过树高差限定)和红黑树(通过颜色标识限定)等平衡二叉树。</p>
]]></content>
      
        
        <tags>
            
            <tag> javascript </tag>
            
        </tags>
        
    </entry>
    
    <entry>
      <title><![CDATA[.htaccess文件详解]]></title>
      <url>/2018/01/06/htaccess/</url>
      <content type="html"><![CDATA[<blockquote>
<p><strong>.htaccess的基本作用</strong></p>
</blockquote>
<p>.htaccess是一个纯文本文件，它里面存放着Apache服务器配置相关的指令。</p>
<p>.htaccess主要的作用有：<strong>URL重写、自定义错误页面、MIME类型配置以及访问权限控制等</strong>。</p>
<p>主要体现在伪静态的应用、图片防盗链、自定义404错误页面、阻止/允许特定IP/IP段、目录浏览与主页、禁止访问指定文件类型、文件密码保护等。</p>
<p>.htaccess的用途范围主要针对当前目录。</p>
<blockquote>
<p><strong>如何启用.htaccess的配置</strong></p>
</blockquote>
<p>启用.htaccess，需要修改httpd.conf文件，启用AllowOverride，并可以用AllowOverride限制特定命令的使用。</p>
<pre><code>&lt;Directory /&gt;
Options FollowSymLinks
AllowOverride None
&lt;/Directory&gt;
</code></pre><p>改为：</p>
<pre><code>&lt;Directory /&gt;
Options FollowSymLinks
AllowOverride All
&lt;/Directory&gt;
</code></pre><p>如果需要使用.htaccess以外的其他文件名，可以用AccessFileName指令来改变。例如，需要使用.config ，则可以在服务器配置文件中按以下方法配置：</p>
<pre><code>AccessFileName .config
</code></pre><blockquote>
<p><strong>.htaccess访问控制</strong></p>
</blockquote>
<p>限制用户访问一些关键目录</p>
<pre><code>&lt;Files  ~ &quot;^.*\.([Ll][Oo][Gg])|([eE][xX][eE])&quot;&gt;
 Order allow,deny
 Deny from all
&lt;/Files&gt;
</code></pre><ul>
<li>Files后的波浪线表示启用“正则表达式”</li>
<li>Order命令：通过Allow,Deny参数，Apache首先找到并应用Allow命令，然后应用Deny命令，以阻止所有访问，也可以使用Deny,Allow</li>
</ul>
<blockquote>
<p><strong>URL重写</strong></p>
</blockquote>
<pre><code># 将 RewriteEngine 模式打开 
RewriteEngine On 
# Rewrite 系统规则请勿修改 
RewriteRule ^p/([0-9]+)\.html$ index.php?post_id=$1
RewriteRule ^u-(username|uid)-(.+)\.html$ space.php?$1=$2
</code></pre><blockquote>
<p><strong>配置错误页面</strong></p>
</blockquote>
<pre><code># custom error documents
ErrorDocument 401 /err/401.php
ErrorDocument 403 /err/403.php
ErrorDocument 404 /err/404.php
ErrorDocument 500 /err/500.php
</code></pre><blockquote>
<p><strong>常用命令及配置</strong></p>
</blockquote>
<ul>
<li>禁止显示目录列表</li>
</ul>
<p>有些时候，由于某种原因，你的目录里没有index文件，这意味着当有人在浏览器地址栏键入了该目录的路径，该目录下所有的文件都会显示出来，这会给你的网站留下安全隐患。</p>
<p>为避免这种情况（而不必创建一堆的新index文件），你可以在你的.htaccess文档中键入以下命令，用以阻止<br>目录列表的显示：</p>
<pre><code>Options -Indexes
</code></pre><ul>
<li>阻止/允许特定的IP地址</li>
</ul>
<p>某些情况下，你可能只想允许某些特定IP的用户可以访问你的网站，或者想封禁某些特定的IP地址（例如：将低级用户隔离于你的信息版面外）。当然，这只在你知道你想拦截的IP地址时才有用，然而现在网上的大多数用户都使用动态IP地址，所以这并不是限制使用的常用方法。</p>
<p>使用以下命令封禁一个IP地址(只指明了其中的几个，则可以封禁整个网段的地址)：</p>
<pre><code>deny from 000.000.000.000
</code></pre><p>使用以下命令允许一个IP地址访问网站(只指明了其中的几个，则可以允许整个网段的地址)：</p>
<pre><code>allow from 000.000.000.000
</code></pre><p>阻止所有人访问该目录，则可以使用(并不影响脚本程序使用这个目录下的文档)：</p>
<pre><code>deny from all
</code></pre><ul>
<li>替换index文件</li>
</ul>
<p>也许你不想一直使用index.htm或index.html作为目录的索引文件。如果你的站点使用PHP文件，你可能会想使用 index.php来作为该目录的索引文档。当然也不必局限于“index”文档，如果你愿意，使用.htaccess你甚至能够设置 foofoo.balh来作为你的索引文档！</p>
<p>这些互为替换的索引文件可以排成一个列表，服务器会从左至右进行寻找，检查哪个文档在真实的目录中存在。如果一个也找不到，它将会把目录列表显示出来（除非你已经关闭了显示目录文件列表）。</p>
<pre><code>DirectoryIndex index.php index.php3 messagebrd.pl index.html index.htm
</code></pre><ul>
<li>重定向(rewrite)</li>
</ul>
<p>.htaccess 最有用的功能之一就是将请求重定向到同站内或站外的不同文档。这在你改变了一个文件名称，但仍然想让用户用旧地址访问到它时，变的极为有用。另一个应用是重定向到一个长URL。以下是一个重定向文件的例子：</p>
<pre><code>Redirect /location/from/root/file.ext http:///new/file/location.xyz
</code></pre><p>你也可以使用.htaccess重定向整个网站的目录。假如你的网站上有一个名为olddirectory的目录，并且你已经在一个新网站http: ///newdirectory/上建立了与上相同的文档，你可以将旧目录下所有的文件做一次重定向而不必一一声明：</p>
<pre><code>Redirect /olddirectory http:　///newdirectory
</code></pre><blockquote>
<p><strong>安全配置</strong></p>
</blockquote>
<ul>
<li>.htaccess链接盗用保护</li>
</ul>
<pre><code>RewriteBase /  
RewriteCond %{HTTP_REFERER} !^$  
RewriteCond %{HTTP_REFERER} !^http://(www.)?aqee.net/.*$ [NC]  
RewriteRule .(gif|jpg|swf|flv|png)$ /feed/ [R=302,L] 
</code></pre><ul>
<li>防黑客</li>
</ul>
<pre><code>RewriteEngine On  

#proc/self/environ
RewriteCond %{QUERY_STRING} proc/self/environ [OR]  

#阻止脚本企图通过URL修改mosConfig值  
RewriteCond %{QUERY_STRING} mosConfig_[a-zA-Z_]{1,21}(=|%3D) [OR]  

#阻止脚本通过URL传递的base64_encode垃圾信息  
RewriteCond %{QUERY_STRING} base64_encode.*(.*) [OR]  

#阻止在URL含有&lt;script&gt;标记的脚本  
RewriteCond %{QUERY_STRING} (&lt;|%3C).*script.*(&gt;|%3E) [NC,OR]  

#阻止企图通过URL设置PHP的GLOBALS变量的脚本  
RewriteCond %{QUERY_STRING} GLOBALS(=|[|%[0-9A-Z]{0,2}) [OR]  

#阻止企图通过URL设置PHP的_REQUEST变量的脚本  
RewriteCond %{QUERY_STRING} _REQUEST(=|[|%[0-9A-Z]{0,2})  

#把所有被阻止的请求转向到403禁止提示页面！  
RewriteRule ^(.*)$ index.php [F,L] 
</code></pre><ul>
<li>阻止访问你的 .htaccess 文件或者指定类型的文件</li>
</ul>
<pre><code># 保护你的 htaccess 文件  
&lt;Files .htaccess&gt;  
order allow,deny  
deny from all  
&lt;/Files&gt;  

# 阻止查看指定的文件  
&lt;Files secretfile.jpg&gt;  
order allow,deny  
deny from all  
&lt;/Files&gt;  

# 多种文件类型  
&lt;FilesMatch “.(htaccess|htpasswd|ini|phps|fla|psd|log|sh)$”&gt;  
 Order Allow,Deny  
Deny from all  
&lt;/FilesMatch&gt;
</code></pre><ul>
<li>禁止脚本执行</li>
</ul>
<pre><code># 禁止某些目录里的脚本执行权限  
AddHandler cgi-script .php .pl .py .jsp .asp .htm .shtml .sh .cgi  
Options -ExecCGI
</code></pre><blockquote>
<p><strong>其他常用配置</strong></p>
</blockquote>
<ul>
<li>屏蔽下载对话框</li>
</ul>
<pre><code>AddType application/octet-stream .pdf  
AddType application/octet-stream .zip  
AddType application/octet-stream .mov 
</code></pre><ul>
<li>省去www前缀</li>
</ul>
<pre><code>RewriteEngine On  
RewriteBase /  
RewriteCond %{HTTP_HOST} ^www.aqee.net [NC]  
RewriteRule ^(.*)$ http://aqee.net/$1 [L,R=301]
</code></pre><ul>
<li>压缩文件</li>
</ul>
<pre><code># 压缩 text, html, javascript, css, xml:  
AddOutputFilterByType DEFLATE text/plain  
AddOutputFilterByType DEFLATE text/html  
AddOutputFilterByType DEFLATE text/xml  
AddOutputFilterByType DEFLATE text/css  
AddOutputFilterByType DEFLATE application/xml  
AddOutputFilterByType DEFLATE application/xhtml+xml  
AddOutputFilterByType DEFLATE application/rss+xml  
AddOutputFilterByType DEFLATE application/javascript  
AddOutputFilterByType DEFLATE application/x-javascript 
</code></pre><ul>
<li>缓存文件</li>
</ul>
<pre><code>&lt;FilesMatch “.(flv|gif|jpg|jpeg|png|ico|swf|js|css|pdf)$”&gt;  
Header set Cache-Control “max-age=2592000″  
&lt;/FilesMatch&gt; 
</code></pre><ul>
<li>对某些文件类型禁止使用缓存</li>
</ul>
<pre><code># 显式的规定对脚本和其它动态文件禁止使用缓存  
&lt;FilesMatch “.(pl|php|cgi|spl|scgi|fcgi)$”&gt;  
Header unset Cache-Control  
&lt;/FilesMatch&gt; 
</code></pre>]]></content>
      
        
        <tags>
            
            <tag> 服务器 </tag>
            
        </tags>
        
    </entry>
    
    <entry>
      <title><![CDATA[H5实现摇一摇]]></title>
      <url>/2017/11/20/devicemotion/</url>
      <content type="html"><![CDATA[<blockquote>
<p><strong>关于deviceMotion</strong></p>
</blockquote>
<p>html5提供的DOM事件来获得设备物理方向及运动的信息，包括：陀螺仪、罗盘及加速计。</p>
<p><strong>deviceorientation</strong>，其提供设备的物理方向信息，表示为一系列本地坐标系的旋角。</p>
<p><strong>devicemotion</strong>，其提供设备的加速信息，表示为定义在设备上的坐标系中的卡尔迪坐标。其还提供了设备在坐标系中的自转速率。</p>
<p><strong>compassneedscalibration</strong>，其用于使用罗盘信息校准上述事件。</p>
<blockquote>
<p><strong>实现原理</strong></p>
</blockquote>
<p>从各个内置传感器那里获取未经修改的传感数据，并观测或响应设备各种运动和角度变化。这些传感器包括陀螺仪、加速器和磁力仪(罗盘)</p>
<p>加速器和陀螺仪的数据都是描述沿着设备三个方向轴上的位置，X方向从设备的左边(负)到右边(正)，Y方向则是由设备的底部(-)到顶部(+)，而Z方向为垂直于屏幕由设备的背面(-)到正面(+)</p>
<p><img src="https://github.com/Leooman/Leooman.github.io/blob/master/img/device.png?raw=true" alt="device"></p>
<p>DeviceMotionEvent 会在设备发生有意义的摆动(或运动)时产生.事件对象封装有产生的间距值,旋转率,和设备加速度.</p>
<p>加速度的计算方式是重力和用户产生的两个加速度矢量之和.设备是通过 陀螺仪和加速计来区别这两者的.</p>
<p>通过DeviceMotion对设备运动状态的判断，则可以帮助我们在网页上就实现“摇一摇”的交互效果。</p>
<blockquote>
<p><strong>事件监听</strong></p>
</blockquote>
<pre><code>if ((window.DeviceMotionEvent) {
  window.addEventListener(&apos;devicemotion&apos;, deviceMotionHandler, false);
} else {
  document.getElementById(&quot;dmEvent&quot;).innerHTML = &quot;Not supported on your device.&quot;
}
</code></pre><blockquote>
<p><strong>获取重力加速度</strong></p>
</blockquote>
<pre><code>var acceleration = eventData.accelerationIncludingGravity;
</code></pre><blockquote>
<p><strong>deviceMotionHandler事件</strong></p>
</blockquote>
<pre><code>var SHAKE_THRESHOLD = 3000;
var last_update = 0;
var x = y = z = last_x = last_y = last_z = 0;      
function deviceMotionHandler(eventData) {        
  var acceleration =eventData.accelerationIncludingGravity;
  var curTime = new Date().getTime();       
  if ((curTime - last_update)&gt; 300) {                
      var diffTime = curTime -last_update;
      last_update = curTime;       
      x = acceleration.x;
      y = acceleration.y;
      z = acceleration.z;       
      var speed = Math.abs(x +y + z - last_x - last_y - last_z) / diffTime * 10000;          
      if (speed &gt; SHAKE_THRESHOLD) {
           alert(&quot;shaked&quot;);
      }
      last_x = x;
      last_y = y;
      last_z = z;
    }
}
</code></pre><blockquote>
<p><strong>DEMO</strong></p>
</blockquote>
<p><a href="https://github.com/Leooman/Leooman.github.io/issues/2" target="_blank" rel="noopener">代码测试地址：https://github.com/Leooman/Leooman.github.io/issues/2</a> </p>
]]></content>
      
        
        <tags>
            
            <tag> HTML5 </tag>
            
        </tags>
        
    </entry>
    
    <entry>
      <title><![CDATA[图片预加载]]></title>
      <url>/2017/10/31/preload/</url>
      <content type="html"><![CDATA[<blockquote>
<p><strong>预加载</strong></p>
</blockquote>
<p>图片预先加载到浏览器中，访问者便可顺利地在你的网站上冲浪，并享受到极快的加载速度。这对图片画廊及图片占据很大比例的网站来说十分有利，它保证了图片快速、无缝地发布，也可帮助用户在浏览你网站内容时获得更好的用户体验。</p>
<blockquote>
<p><strong>用CSS和JavaScript实现预加载</strong></p>
</blockquote>
<ul>
<li>单纯使用CSS，可容易、高效地预加载图片</li>
</ul>
<pre><code>#preload-01 { background: url(http://domain.tld/image-01.png) no-repeat -9999px -9999px; }  
#preload-02 { background: url(http://domain.tld/image-02.png) no-repeat -9999px -9999px; }  
#preload-03 { background: url(http://domain.tld/image-03.png) no-repeat -9999px -9999px; }
</code></pre><p>通过CSS的background属性将图片预加载到屏幕外的背景上。只要这些图片的路径保持不变，当它们在Web页面的其他地方被调用时，浏览器就会在渲染过程中使用预加载（缓存）的图片。简单、高效，不需要任何JavaScript。</p>
<p>使用该法加载的图片会同页面的其他内容一起加载，增加了页面的整体加载时间。</p>
<ul>
<li>增加了一些JavaScript代码，来推迟预加载的时间，直到页面加载完毕</li>
</ul>
<pre><code>function preloader() {  
    if (document.getElementById) {  
        document.getElementById(&quot;preload-01&quot;).style.background = &quot;url(http://domain.tld/image-01.png) no-repeat -9999px -9999px&quot;;  
        document.getElementById(&quot;preload-02&quot;).style.background = &quot;url(http://domain.tld/image-02.png) no-repeat -9999px -9999px&quot;;  
        document.getElementById(&quot;preload-03&quot;).style.background = &quot;url(http://domain.tld/image-03.png) no-repeat -9999px -9999px&quot;;  
    }  
}  
function addLoadEvent(func) {  
    var oldonload = window.onload;  
    if (typeof window.onload != &apos;function&apos;) {  
        window.onload = func;  
    } else {  
        window.onload = function() {  
            if (oldonload) {  
                oldonload();  
            }  
            func();  
        }  
    }  
}  
addLoadEvent(preloader);
</code></pre><p>addLoadEvent()函数来延迟preloader()函数的加载时间，直到页面加载完毕。</p>
<blockquote>
<p><strong>仅使用JavaScript实现预加载</strong></p>
</blockquote>
<p>上述方法有时确实很高效，但我们逐渐发现它在实际实现过程中会耗费太多时间。</p>
<ul>
<li>方法一</li>
</ul>
<p>只需简单编辑、加载所需要图片的路径与名称即可，该方法尤其适用预加载大量的图片(比如图片画廊)。</p>
<pre><code>&lt;div class=&quot;hidden&quot;&gt;  
&lt;script type=&quot;text/javascript&quot;&gt; 
    var images = new Array()  
    function preload() {  
        for (i = 0; i &lt; preload.arguments.length; i++) {  
            images[i] = new Image()  
            images[i].src = preload.arguments[i]  
        }  
    }  
    preload(  
        &quot;http://domain.tld/gallery/image-001.jpg&quot;,  
        &quot;http://domain.tld/gallery/image-002.jpg&quot;,  
        &quot;http://domain.tld/gallery/image-003.jpg&quot;  
    ) 
&lt;/script&gt;  
&lt;/div&gt;
</code></pre><ul>
<li><p>方法二</p>
  <div class="hidden"><br>  <script type="text/javascript"><br>      if (document.images) {<br>          img1 = new Image();<br>          img2 = new Image();<br>          img3 = new Image();<br>          img1.src = “<a href="http://domain.tld/path/to/image-001.gif&quot;">http://domain.tld/path/to/image-001.gif&quot;</a>;<br>          img2.src = “<a href="http://domain.tld/path/to/image-002.gif&quot;">http://domain.tld/path/to/image-002.gif&quot;</a>;<br>          img3.src = “<a href="http://domain.tld/path/to/image-003.gif&quot;">http://domain.tld/path/to/image-003.gif&quot;</a>;<br>      }<br>  </script><br>  </div>

</li>
</ul>
<p>每加载一个图片都需要创建一个变量(有点烦)</p>
<pre><code>function preloader() {  
    if (document.images) {  
        var img1 = new Image();  
        var img2 = new Image();  
        var img3 = new Image();  
        img1.src = &quot;http://domain.tld/path/to/image-001.gif&quot;;  
        img2.src = &quot;http://domain.tld/path/to/image-002.gif&quot;;  
        img3.src = &quot;http://domain.tld/path/to/image-003.gif&quot;;  
    }  
}  
function addLoadEvent(func) {  
    var oldonload = window.onload;  
    if (typeof window.onload != &apos;function&apos;) {  
        window.onload = func;  
    } else {  
        window.onload = function() {  
            if (oldonload) {  
                oldonload();  
            }  
            func();  
        }  
    }  
}  
addLoadEvent(preloader);
</code></pre><blockquote>
<p><strong>使用Ajax实现预加载</strong></p>
</blockquote>
<p>利用DOM，不仅仅预加载图片，还会预加载CSS、JavaScript等相关的东西。使用Ajax，比直接使用JavaScript，优越之处在于JavaScript和CSS的加载不会影响到当前页面。该方法简洁、高效</p>
<pre><code>window.onload = function() {  
    setTimeout(function() {  
        // XHR to request a JS and a CSS  
        var xhr = new XMLHttpRequest();  
        xhr.open(&apos;GET&apos;, &apos;http://domain.tld/preload.js&apos;);  
        xhr.send(&apos;&apos;);  
        xhr = new XMLHttpRequest();  
        xhr.open(&apos;GET&apos;, &apos;http://domain.tld/preload.css&apos;);  
        xhr.send(&apos;&apos;);  
        // preload image  
        new Image().src = &quot;http://domain.tld/preload.png&quot;;  
    }, 1000);  
};
//1000毫秒的超时是为了防止脚本挂起，而导致正常页面出现功能问题。
</code></pre><p>对比JavaScript来实现该加载过程：</p>
<p>通过DOM创建三个元素来实现三个文件的预加载。正如上面提到的那样，使用Ajax，加载文件不会应用到加载页面上</p>
<pre><code>window.onload = function() {  

    setTimeout(function() {  

        // reference to &lt;head&gt;  
        var head = document.getElementsByTagName(&apos;head&apos;)[0];  

        // a new CSS  
        var css = document.createElement(&apos;link&apos;);  
        css.type = &quot;text/css&quot;;  
        css.rel  = &quot;stylesheet&quot;;  
        css.href = &quot;http://domain.tld/preload.css&quot;;  

        // a new JS  
        var js  = document.createElement(&quot;script&quot;);  
        js.type = &quot;text/javascript&quot;;  
        js.src  = &quot;http://domain.tld/preload.js&quot;;  

        // preload JS and CSS  
        head.appendChild(css);  
        head.appendChild(js);  

        // preload image  
        new Image().src = &quot;http://domain.tld/preload.png&quot;;  

    }, 1000);  

};
</code></pre>]]></content>
      
        
        <tags>
            
            <tag> web </tag>
            
        </tags>
        
    </entry>
    
    <entry>
      <title><![CDATA[web前端兼容性问题]]></title>
      <url>/2017/10/30/hack/</url>
      <content type="html"><![CDATA[<blockquote>
<p><strong>event.x与event.y问题</strong></p>
</blockquote>
<p>IE下,event对象有x,y属性,但是没有pageX,pageY属性;</p>
<p>Firefox下,event对象有pageX,pageY属性,但是没有x,y属性. </p>
<blockquote>
<p><strong>input.type属性问题</strong></p>
</blockquote>
<p>IE下input.type属性为只读；但是Firefox下input.type属性为读写。</p>
<p>不修改input.type属性。如果必须要修改，可以先隐藏原来的input，然后在同样的位置再插入一个新的input元素。</p>
<blockquote>
<p><strong>event.srcElement问题</strong></p>
</blockquote>
<p>IE下，even对象有srcElement属性，但是没有target属性；Firefox下，even对象有target属性，但是没有srcElement属性。</p>
<p>使用srcObj =event.srcElement ?event.srcElement : event.target;</p>
<blockquote>
<p><strong>事件委托方法</strong></p>
</blockquote>
<p>IE下，使用document.body.onload= inject; 其中functioninject()在这之前已被实现；在Firefox下，使用document.body.onload= inject();</p>
<p>统一使用document.body.onload=newFunction(’inject()’); 或者document.body.onload = function(){/<em> 这里是代码 </em>/}<br>[注意] Function和function的区别。</p>
<blockquote>
<p><strong>对象宽高赋值问题</strong></p>
</blockquote>
<p>FireFox中类似obj.style.height =imgObj.height的语句无效。</p>
<blockquote>
<p><strong>cursor:hand和cursor:pointer</strong></p>
</blockquote>
<p>firefox不支持hand，ie/firefox都支持pointer</p>
<blockquote>
<p><strong>innerText在IE中能正常工作，但在FireFox中却不行</strong></p>
</blockquote>
<pre><code>if(navigator.appName.indexOf(&quot;Explorer&quot;)&gt;-1){
   document.getElementById(&apos;element&apos;).innerText=&quot;my text&quot;;
}else{
   document.getElementById(&apos;element&apos;).textContent=&quot;my text&quot;;
}
</code></pre><blockquote>
<p><strong>CSS透明</strong></p>
</blockquote>
<p>IE：filter:progid:DXImageTransform.Microsoft.Alpha(style=0,opacity=60)</p>
<p>FF：opacity:0.6</p>
<blockquote>
<p><strong>css中的width和padding</strong></p>
</blockquote>
<p>在IE7和FF中width宽度不包括padding，在Ie6中包括padding.</p>
<blockquote>
<p><strong>FF和IE BOX模型解释不一致导致相差2px</strong></p>
</blockquote>
<p>box.style{width:100;border1px;} </p>
<p>ie理解为box.width =100 </p>
<p>ff理解为box.width =100 + 1*2 = 102  //加上边框2px</p>
<p>解决方法：div{margin:30px!important;margin:28px;}</p>
<p>注意这两个margin的顺序一定不能写反， IE不能识别!important这个属性，但别的浏览器可以识别。所以在IE下其实解释成这样：div{maring:30px;margin:28px}<br>重复定义的话按照最后一个来执行，所以不可以只写margin:XXpx!important;</p>
<blockquote>
<p><strong>IE5 和IE6的BOX解释不一致</strong></p>
</blockquote>
<p>IE5下div{width:300px;margin:0 10px 0 10px;}div 的宽度会被解释为300px-10px(右填充)-10px(左填充)，最终div的宽度为280px，</p>
<p>而在IE6和其他浏览器上宽度则是以 300px+10px(右填充)+10px(左填充)=320px来计算的。</p>
<p>这时我们可以做如下修改div{width:300px!important;width /**/:340px;margin:0 10px 0 10px}</p>
<blockquote>
<p><strong>元素水平居中问题</strong></p>
</blockquote>
<p>FF: margin:0auto;</p>
<p>IE: 父级{ text-align:center; }</p>
<blockquote>
<p><strong>margin加倍的问题</strong></p>
</blockquote>
<p>设置为float的div在ie下设置的margin会加倍。这是一个ie6都存在的bug。解决方案是在这个div里面加上display:inline;</p>
<pre><code>&lt;div id=&quot;imfloat&quot;&gt;

#imfloat{ 
    float:left; 
    margin:5px;/*IE下理解为10px*/ 
    display:inline;/*IE下再理解为5px*/
}
</code></pre><blockquote>
<p><strong>IE6下图片下方有空隙产生</strong></p>
</blockquote>
<p>1:定义 img 为 display:block</p>
<p>2:定义 vertical-align 属性值为 vertical-align:top | bottom |middle |text-bottom</p>
<p>3:设置父容器的字体大小为零，font-size:0</p>
<blockquote>
<p><strong>IE6下空标签高度问题</strong></p>
</blockquote>
<p>一个空 div 如果高度设置为 0 到 19px，IE6 下高度默认始终 19px</p>
<pre><code>.c{background-color:#f00;height:0px;/*给定任何小于 20px 的高度 */}
&lt;div class=&quot;c&quot;&gt;&lt;/div&gt;
</code></pre><p>如果不让它默认为19px而是0px.解决方法有 3 种：</p>
<p>1.css 里面加上 overflow:hidden;</p>
<p>2.div 里面加上注释</p>
<pre><code>&lt;div class=&quot;c&quot;&gt;&lt;!– –&gt;&lt;/div&gt;
</code></pre><p>3.css 里面加上 line-height:0;然后 div 里面加上&amp;#nbsp</p>
<pre><code>&lt;div class=&quot;c&quot;&gt;&amp;nbsp;&lt;/div&gt;
</code></pre><blockquote>
<p><strong>页面的最小宽度</strong></p>
</blockquote>
<p>IE不识别min，要实现最小宽度，可用下面的方法：</p>
<pre><code>#container{ min-width: 600px;_width:expression(document.body.clientWidth＜ 600? &quot;600px&quot;: &quot;auto&quot; );}
</code></pre><p>第一个min-width是正常的；但第2行的width使用了Javascript，这只有IE才认得，这也会让你的HTML文档不太正规。它实际上通过Javascript的判断来实现最小宽度。</p>
<blockquote>
<p><strong>页面的最小高度</strong></p>
</blockquote>
<pre><code>#test {min-height:100px;
    _height:100px;
    overflow: visible;
}
</code></pre><blockquote>
<p><strong>IE6 Png图片不透明问题</strong></p>
</blockquote>
<pre><code>.pngbackground {
    position:relative;
    background:url(your.png);
    _background: none;
    _filter:progid:DXImageTransform.Microsoft.AlphaImageLoader(src=&apos;your.png&apos;,sizingMethod=&apos;scale&apos;);
}
</code></pre><blockquote>
<p><strong>清除浮动</strong></p>
</blockquote>
<pre><code>/* Clear Fix */ 
.clearfix:after { content:&quot;.&quot;; display:block; height:0; clear:both;visibility:hidden; } 
.clearfix { display:inline-block; *zoom:1;} 
/* Hide from IE Mac */ 
.clearfix {display:block;} 
/* End hide from IE Mac */ 
/* end of clearfix */
</code></pre><blockquote>
<p><strong>当子元素浮动且未知高度时，父容器适应子元素的高度</strong></p>
</blockquote>
<p>父窗口加上 overflow:auto;zoom:1;这两个样式属性，overflow:auto;是让父容器来自适应内部容器的高度，zoom:1;是为了兼容 IE6 而使用的 CSS HACK。zoom:1;通不过 W3C 的验证，这也是遗憾的一点，幸好 IE 支持<code>&lt;!--[if IE]&gt;</code>这种写法，可以专门针对 IE 来写单独的样式，所以可以把这个属性写在页面内的<code>&lt;!--[if IE]&gt;</code>中，这样应该可以通过验证了</p>
<blockquote>
<p><strong>IE无法设置滚动条颜色</strong></p>
</blockquote>
<p>解决办法是将body换成html</p>
<pre><code>html { 
    scrollbar-face-color:#f6f6f6; 
    scrollbar-highlight-color:#fff; 
    scrollbar-shadow-color:#eeeeee; 
    scrollbar-3dlight-color:#eeeeee; 
    scrollbar-arrow-color:#000; 
    scrollbar-track-color:#fff; 
    scrollbar-darkshadow-color:#fff; 
} 
</code></pre><blockquote>
<p><strong>超链接访问过后hover样式就不出现的问题</strong></p>
</blockquote>
<p>被点击访问过的超链接样式不在具有hover和active,改变CSS属性的排列顺序: L-V-H-A</p>
<pre><code>a:link {} 
a:visited {} 
a:hover {} 
a:active {}
</code></pre><blockquote>
<p><strong>移除超级链接的虚线</strong></p>
</blockquote>
<p>FireFox 下,当你点击一个超链接时会在外围出现一个虚线轮廓</p>
<pre><code>a{outline: none;}
</code></pre><blockquote>
<p><strong>css hack</strong></p>
</blockquote>
<table>
<thead>
<tr>
<th style="text-align:left">区别符号</th>
<th style="text-align:center">FireFox</th>
<th style="text-align:center">IE9</th>
<th style="text-align:center">IE8</th>
<th style="text-align:center">IE7</th>
<th style="text-align:center">IE6</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left">\0(后面紧跟)</td>
<td style="text-align:center">×</td>
<td style="text-align:center">√</td>
<td style="text-align:center">√</td>
<td style="text-align:center">×</td>
<td style="text-align:center">×</td>
</tr>
<tr>
<td style="text-align:left">\0/0(后面紧跟)</td>
<td style="text-align:center">×</td>
<td style="text-align:center">×</td>
<td style="text-align:center">√</td>
<td style="text-align:center">×</td>
<td style="text-align:center">×</td>
</tr>
<tr>
<td style="text-align:left">\9(后面空格)</td>
<td style="text-align:center">×</td>
<td style="text-align:center">×</td>
<td style="text-align:center">√</td>
<td style="text-align:center">√</td>
<td style="text-align:center">√</td>
</tr>
<tr>
<td style="text-align:left">\9(后面紧跟)</td>
<td style="text-align:center">×</td>
<td style="text-align:center">√</td>
<td style="text-align:center">√</td>
<td style="text-align:center">√</td>
<td style="text-align:center">√</td>
</tr>
<tr>
<td style="text-align:left">*</td>
<td style="text-align:center">×</td>
<td style="text-align:center">×</td>
<td style="text-align:center">×</td>
<td style="text-align:center">√</td>
<td style="text-align:center">√</td>
</tr>
<tr>
<td style="text-align:left">+</td>
<td style="text-align:center">×</td>
<td style="text-align:center">×</td>
<td style="text-align:center">×</td>
<td style="text-align:center">√</td>
<td style="text-align:center">√</td>
</tr>
<tr>
<td style="text-align:left">_(IE6专用)</td>
<td style="text-align:center">×</td>
<td style="text-align:center">×</td>
<td style="text-align:center">×</td>
<td style="text-align:center">×</td>
<td style="text-align:center">√</td>
</tr>
<tr>
<td style="text-align:left">!important</td>
<td style="text-align:center">√</td>
<td style="text-align:center">√</td>
<td style="text-align:center">√</td>
<td style="text-align:center">√</td>
<td style="text-align:center">√(识别但不提高优先级别)</td>
</tr>
</tbody>
</table>
<pre><code>height:300px;*height:200px;_height:100px;
</code></pre><p>IE6解析为100px，IE7解析为200px</p>
<blockquote>
<p><strong>判断IE版本</strong></p>
</blockquote>
<pre><code>&lt;!--[if lte IE 6]&gt;
&lt;![endif]--&gt;
IE6 及其以下版本可见
&lt;!--[if lte IE 7]&gt;
&lt;![endif]--&gt;
IE7 及其以下版本可见
&lt;!--[if IE 6]&gt;
&lt;![endif]--&gt;
只有 IE6 版本可见
&lt;![if !IE]&gt;
&lt;![endif]&gt;
除了 IE 以外的版本
&lt;!--[if lt IE 8]&gt;
&lt;![endif]--&gt;
IE8 及其以下的版本可见
&lt;!--[if gte IE 7]&gt;
&lt;![endif]--&gt;
IE7 及其以下的版本可见
</code></pre>]]></content>
      
        
        <tags>
            
            <tag> web </tag>
            
        </tags>
        
    </entry>
    
    <entry>
      <title><![CDATA[图片懒加载]]></title>
      <url>/2017/10/18/lazyload/</url>
      <content type="html"><![CDATA[<blockquote>
<p><strong>场景</strong></p>
</blockquote>
<p>在实际的项目开发中，我们通常会遇见这样的场景：一个页面有很多图片，而首屏出现的图片大概就一两张，那么我们还要一次性把所有图片都加载出来吗？显然这是愚蠢的，不仅影响页面渲染速度，还浪费带宽。这也就是们通常所说的首屏加载，技术上现实其中要用的技术就是图片懒加载–到可视区域再加载</p>
<blockquote>
<p><strong>优势</strong></p>
</blockquote>
<p>减少请求数，防止页面一次性向服务器响应大量请求导致服务器响应慢，页面卡顿或崩溃等问题</p>
<p>页面加载速度快、可以减轻服务器的压力、节约了流量、用户体验好</p>
<blockquote>
<p><strong>实现原理</strong></p>
</blockquote>
<p>页面中的img元素，如果没有src属性，浏览器就不会发出请求去下载图片，只有通过javascript设置了图片路径，浏览器才会发送请求。</p>
<p>1)先在页面中把所有的图片统一使用一张占位图进行占位，不要将图片地址放到src属性中，而是放到其它属性(data-original)中。</p>
<pre><code>&lt;img data-url=&quot;1.jpg&quot; src=&quot;blank.png&quot; alt=&quot;测试懒加载图片&quot;/&gt;
</code></pre><p>2)页面加载完成后，根据scrollTop判断图片是否在用户的视野内，如果在，则将data-original属性中的值取出存放到src属性中。<br>3)在滚动事件中重复判断图片是否进入视野，如果进入，则将data-original属性中的值取出存放到src属性中。</p>
<blockquote>
<p><strong>屏幕可视窗口大小</strong></p>
</blockquote>
<pre><code>**原生方法**：
    window.innerHeight 标准浏览器及IE9+ ||
    document.documentElement.clientHeight 标准浏览器及低版本IE标准模式 ||
    document.body.clientHeight  低版本混杂模式
**jQuery方法**： 
    $(window).height();
</code></pre><blockquote>
<p><strong>滚动条滚动的距离</strong></p>
</blockquote>
<pre><code>**原生方法**：
      window.pagYoffset 标准浏览器及IE9+ ||
      document.documentElement.scrollTop 兼容ie低版本的标准模式 ||
      document.body.scrollTop 兼容混杂模式；
**jQuery方法**：
      $(document).scrollTop();
</code></pre><blockquote>
<p><strong>获取元素的尺寸</strong></p>
</blockquote>
<pre><code>$(o).width() = o.style.width;
$(o).innerWidth() = o.style.width+o.style.padding;
$(o).outerWidth() = o.offsetWidth = o.style.width+o.style.padding+o.style.border;
$(o).outerWidth(true) = o.style.width+o.style.padding+o.style.border+o.style.margin;
</code></pre><ul>
<li>注意</li>
</ul>
<p>要使用原生的style.xxx方法获取属性，这个元素必须已经有内嵌的样式，如<code>&lt;div style=&quot;....&quot;&gt;&lt;/div&gt;</code>；</p>
<p>如果原先是通过外部或内部样式表定义css样式，必须使用<code>o.currentStyle[xxx] || document.defaultView.getComputedStyle(0)[xxx]</code>来获取样式值。</p>
<blockquote>
<p><strong>获取元素的位置信息</strong></p>
</blockquote>
<ul>
<li><p>jQuery：</p>
<p>  $(o).offset().top元素距离文档顶的距离</p>
<p>  $(o).offset().left元素距离文档左边缘的距离</p>
</li>
<li><p>原生：</p>
<p>  getoffsetTop();</p>
</li>
</ul>
]]></content>
      
        
        <tags>
            
            <tag> web </tag>
            
        </tags>
        
    </entry>
    
  
  
</search>
