```js
//给定一个随机数组，返回递增的最大长度，如[1,3,5,4,7]输出3
function fn(arr){
    let max,tmp = 0
    arr.sort((a,b) => a - b).reduce((total,current) => {
        current === total + 1 ? tmp++ : tmp=1
        if(!max || tmp > max) max = tmp
        return current
    },arr[0] - 1)
    return max
}
```

